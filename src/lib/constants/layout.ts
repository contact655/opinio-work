/**
 * プロフィールの横幅（2026-08-27）。
 *
 * ★**同じプロフィールが、自分で見るときと他人が見るときで幅が違っていた。**
 *   `/u/[id]`（公開）は外枠 1060 ＋ 左右 20 の余白＝**内容 1020**。
 *   `/mypage`（本人）は本文列 1060 ＋ 左右 40 の余白で内側 980、
 *   さらにカードに `maxWidth: 680` がかかっていて **680** しか無かった。
 *
 * ⚠️ **数字を各所に書き写さないこと。** 片方だけ動かすと、また幅がずれる。
 *    ⚠️ `/u/[id]/page.tsx` の `maxWidth: 1060` と `padding: … 20px …` は
 *       **この定数と対になっている**。あちらを変えるならここも変える。
 */

/** 公開プロフィールの内容幅（＝ `/u/[id]` の 1060 − 左右 20×2）。**揃える目標値** */
export const PROFILE_CONTENT_MAX = 1020;

/**
 * `/mypage` の本文列の幅。
 * ⚠️ 本文列には左右 40 の余白があるので、**内容を 1020 にするには 1100 が要る**
 *    （1100 − 40×2 = 1020）。余白を変えるならこの値も変えること。
 */
export const MYPAGE_MAIN_COLUMN = PROFILE_CONTENT_MAX + 80;

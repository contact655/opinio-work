/**
 * 企業からの問い合わせフォーム（/business/contact）の入力上限。
 *
 * ⚠️★**UI と API がこの1箇所を見る。どちらかに数字を書かないこと。**
 *    割れると「入力欄には収まるのに送信すると 400」になり、
 *    しかも**画面はエラーの理由を持っていない**ので原因を追えなくなる。
 *    （CLAUDE.md「選択肢が決まっている値は UI / API / DB の CHECK を3つ揃える」と
 *      同じ理由。この経路は DB を使わないので**揃えるのは UI と API の2層**。）
 *
 * ⚠️ 値の集合の制約ではなく**濃度（長さ）の制約**なので、DB の CHECK は要らない
 *    ——というより、そもそも保存先が DB ではなくメール1本しかない。
 */
export const BUSINESS_CONTACT_LIMITS = {
  company: 100,
  name: 50,
  email: 254,
  phone: 30,
  message: 2000,
} as const;

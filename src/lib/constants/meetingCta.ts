/**
 * カジュアル面談CTA の色（2026-09-06 / 柴さんの選択 = 明るい橙 + 濃い文字）。
 *
 * ── なぜ定数にしたか ────────────────────────────────────────────────────────
 * ⚠️★**同じボタンが12箇所にあり、綴りが3通りに割れていた。**
 *      `#F59E0B 0%, #FB923C 100%` ／ `#F59E0B, #F97316` ／ `#F59E0B, #D97706`
 *    色を直すたびに「1つ取り残す」形だったので、**呼び出し側で色を書かない**ことにした。
 *    `.claude/skills/ui-conventions` の「チップに個別の色を渡せる形にしないこと」と同じ理由。
 *
 * ── ★色の経緯（3回変えている。順番に読むこと）────────────────────────────
 * 2026-08-31  白 on #F59E0B = 2.15 を理由に、背景を #C2410C まで濃くした
 * 2026-09-06  「オレンジにしたい。昔の色」→ 背景を #F59E0B に戻し、文字を #1C1917 に
 * 2026-09-06  「黒文字を白文字へ」→ **文字を白に戻した（現在）**
 *
 * ⚠️★**現在の組み合わせは WCAG AA を満たしていない。**
 *
 *      白 on #F59E0B（グラデーションの最も明るいストップ）= **2.15**
 *      14px の文字に必要なのは 4.5（3.0 が使えるのは 24px 以上か 18.66px 以上の太字）
 *
 *    ⚠️ **文字を大きくしても救えない。** 2.15 は 24px でも 3.0 に届かない。
 *    ⚠️ 参考の実測値: #1C1917 なら 8.14 ／ 中間の #EA580C + 白でも 3.56（届かない）／
 *       白で 4.5 を満たすには #C2410C まで濃くする必要がある（5.18）。
 *
 *    **これは見た目を優先した柴さんの判断で、上の数値を提示したうえで選ばれている。**
 *    ⚠️ 「コントラストが足りないから」を理由に**独断で戻さないこと。**
 *       直すなら、背景を濃くするか文字を濃くするかを**柴さんに確認してから。**
 *
 * ⚠️ 比は**グラデーションの最も明るいストップ**で測る（そこが最悪値になる）。
 *
 * ── 橙のままである ──────────────────────────────────────────────────────────
 * ⚠️ `.claude/skills/ui-conventions`「色の役割」は**オレンジ＝カジュアル面談のみ**。
 *    **役割は変えていない。** 濃さだけを変えた。
 *
 * ⚠️ 「話を聞けます」バッジ（`components/profile/view/TalkableBadge.tsx`）は
 *    **#C2410C の文字色のまま**（#FFF7ED の上で 4.88 ✅）。
 *    あちらは薄い背景に濃い文字、こちらは濃い背景に濃い文字なので、
 *    **同じ橙系だが同じ値にはならない。** 揃えようとして片方を壊さないこと。
 *
 * ⚠️ アウトライン（白地＋橙の枠）にはしない。同じ規約が
 *    「塗り＝主要な遷移／ゴースト＝その場の展開」と定めているため。
 *
 * ⚠️ 3案の実物比較は `/dev/preview/meeting-cta`（本番では404）。
 */
export const MEETING_CTA_BG = "linear-gradient(135deg, #F59E0B 0%, #FB923C 100%)";

/**
 * ボタンの文字・アイコン・ドットの色。
 *
 * ⚠️★**必ず `MEETING_CTA_BG` とセットで使う。** 呼び出し側に色を直書きしないこと ——
 *    2026-09-06 まで12箇所に `color: "#fff"` が散っており、色を変えるたびに
 *    1つ取り残す形だった。定数にしたので、次に変えるときはここ1行で済む。
 *
 * ⚠️ いまの値は白で、背景（明るい橙）との比は **2.15**。上の「色の経緯」を読むこと。
 */
export const MEETING_CTA_FG = "#FFFFFF";

/**
 * 影の色成分だけ。**大きさ（geometry）は呼び出し側で決める** —— 主ボタン・
 * 社員カードの小ボタン・モバイル固定バーで必要な浮き方が違うため。
 *
 * ```ts
 * boxShadow: `0 4px 14px rgba(${MEETING_CTA_SHADOW_RGB},0.35)`
 * ```
 */
export const MEETING_CTA_SHADOW_RGB = "245,158,11";

/**
 * カジュアル面談CTA の色（2026-08-31 / 柴さんの選択 = 案B）。
 *
 * ── なぜ定数にしたか ────────────────────────────────────────────────────────
 * ⚠️★**同じボタンが12箇所にあり、綴りが3通りに割れていた。**
 *      `#F59E0B 0%, #FB923C 100%` ／ `#F59E0B, #F97316` ／ `#F59E0B, #D97706`
 *    色を直すたびに「1つ取り残す」形だったので、**呼び出し側で色を書かない**ことにした。
 *    `.claude/skills/ui-conventions` の「チップに個別の色を渡せる形にしないこと」と同じ理由。
 *
 * ── なぜ濃くしたか ──────────────────────────────────────────────────────────
 * 旧色は **白文字 on #F59E0B（明るい側のストップ）= 2.15**。
 * 13〜14px の文字に必要なのは 4.5（3.0 が使えるのは 24px 以上か 18.66px 以上の太字）。
 * ⚠️★**文字を大きくしても解決しなかった。** 2.15 は 24px でも 3.0 に届かない。
 *
 * 新色は **白 on #C2410C = 5.18** ✅
 *
 * ⚠️ 比は**グラデーションの最も明るいストップ**で測る（そこが最悪値になる）。
 *
 * ── 橙のままである ──────────────────────────────────────────────────────────
 * ⚠️ `.claude/skills/ui-conventions`「色の役割」は**オレンジ＝カジュアル面談のみ**。
 *    **役割は変えていない。** 濃さだけを変えた。
 *
 * ⚠️ **#C2410C は「話を聞けます」バッジ（`components/profile/view/TalkableBadge.tsx`）と
 *    同じ橙。** あちらは #FFF7ED の上で 4.88 と元から基準を満たしており、
 *    **同じ画面に出るバッジとボタンの橙がずれていた**のを揃えた形になる。
 *    → バッジ側の色を変えるときは、ここも一緒に見ること。
 *
 * ⚠️ アウトライン（白地＋橙の枠）にはしない。同じ規約が
 *    「塗り＝主要な遷移／ゴースト＝その場の展開」と定めているため。
 *
 * ⚠️ 3案の実物比較は `/dev/preview/meeting-cta`（本番では404）。
 */
export const MEETING_CTA_BG = "linear-gradient(135deg, #C2410C 0%, #9A3412 100%)";

/**
 * 影の色成分だけ。**大きさ（geometry）は呼び出し側で決める** —— 主ボタン・
 * 社員カードの小ボタン・モバイル固定バーで必要な浮き方が違うため。
 *
 * ```ts
 * boxShadow: `0 4px 14px rgba(${MEETING_CTA_SHADOW_RGB},0.35)`
 * ```
 */
export const MEETING_CTA_SHADOW_RGB = "194,65,12";

/**
 * トップページ（LP）のしきい値と例文。**画面にハードコードしない。**
 *
 * ⚠️ しきい値の定数は `src/lib/constants/` に置く（CLAUDE.md）。
 *    `DISCLOSURE_MAX` を表示側へ直書きして取り残された前例がある。
 */

/**
 * 「募集中の求人」セクションを出す下限。
 *
 * ── なぜ隠すか（2026-09-16）──────────────────────────────────────────────
 * 公開求人は **2件**（掲載22社のうち求人を持つのは1社だけ）。
 * 「2件すべて見る →」は、在庫の薄さを最初に自分から見せる形になる。
 *
 * ⚠️ **「数を隠さない」方針と矛盾しない。**「表示する値を偽らない」ことと
 *    「セクションを出すかどうか」は別の判断で、ここは後者
 *    （出身校ファセットの `SCHOOL_MIN_SCHOOLS` と同じ考え方）。
 *
 * ⚠️ **`/jobs` への導線は消えない。** ヘッダーの「募集」、フッターの「募集を探す」、
 *    モバイルメニューの「募集一覧」の3箇所に残る（2026-09-16 実測）。
 *
 * ⚠️ 在庫が増えれば自動的に表示される。将来ここが出ていない場合は、
 *    バグではなくこの閾値に達していないだけ。
 */
export const LP_JOBS_MIN_TO_SHOW = 10;

/**
 * ヒーローの検索窓の下に出す例文（「たとえば」のチップ）。
 *
 * ⚠️★**押しても即遷移しない。** 検索窓に文が入るだけ。
 *    何を書けば何が返るかを見せるのが目的で、勝手に結果へ飛ばさない。
 *
 * ⚠️★**未ログインで件数1以上になることを実測してから載せること。**
 *    `/search` は語をマスタに解決してから AND で絞るので、
 *    それらしい日本語が必ず当たるとは限らない。実測した反例:
 *      「マーケティングの企業」            → **0件**（「マーケティング」が
 *                                            職種と事業領域の両方に解決し AND で潰れる）
 *      「クラウドインフラの外資系企業」    → **0件**（「インフラ」が職種 SRE/インフラ に解決）
 *
 * ⚠️★**人検索の例文を載せないこと。** 未ログインでは個票が出ず、
 *    件数＋ログイン導線で終わる（`MIN_AGGREGATE_COUNT = 3` 未満だと件数も出ない）。
 *    「調べられます」と言って何にも着かない形になる。
 *
 * ⚠️★**求人が返る例文も載せないこと（2026-09-16 / 柴さんの判断）。**
 *    「年収800万以上の営業の募集」（募集2件）を一度入れて外した。
 *    **`LP_JOBS_MIN_TO_SHOW` で募集セクションを隠した理由と矛盾する** ——
 *    在庫が薄いから畳んだ面を、チップから見せに行くことになる。
 *    ⚠️ 戻すなら募集セクションと同時に判断すること。**チップだけ先に戻さない。**
 *
 * ⚠️★**「人材」を含む問いを載せないこと。** 未解決語の表示が「材業界」と欠ける
 *    不具合がある（`collectUnresolved` が合図の「人」を出現位置すべて伏せ字にするため）。
 *    → docs/todo.md
 *
 * ⚠️ 件数は掲載企業（22社）と公開求人（2件）に直結している。
 *    **企業を1社非掲載にすると0件になりうる。** 掲載を動かしたら測り直すこと。
 *
 * ⚠️★**3つで寂しく見えても、未実測の問いを足さないこと。**
 *    足すなら、未ログインで叩いて件数を確かめてから、この表に1行追記する。
 *
 * ── 実測（2026-09-16 / 未ログイン / localhost:3000 → 本番 Supabase）───────
 *   AI・データの企業              → 企業 4件
 *   外資系のセキュリティ企業      → 企業 2件
 *   日系のHR企業                  → 企業 2件
 */
export const HERO_SEARCH_EXAMPLES: string[] = [
  "AI・データの企業",
  "外資系のセキュリティ企業",
  "日系のHR企業",
];

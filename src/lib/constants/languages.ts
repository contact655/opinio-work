/**
 * 言語（`ow_languages` / `ow_user_languages`）の定数（2026-08-27）。
 *
 * ⚠️ 習熟度は**別ファイル**（`languageProficiency.ts`）。統合しないこと。
 *    あちらは値の集合（UI / API / DB の CHECK の3層）で、こちらは件数の上限。
 */

/**
 * 1人が登録できる言語の上限。
 *
 * ⚠️ **担保するのは UI と API の2層だけ。DB のトリガーは足さない。**
 *    行数（濃度）の制約で、1言語1行の表では CHECK に書けない。
 *    → CLAUDE.md「この規約の適用範囲 —— 『値の集合』の制約だけ」
 *
 * ⚠️ スキル（15）より少ないのは、話せる言語が15個ある人はまずいないため。
 *    増やしたくなったらここだけ変えれば UI と API の両方に効く。
 */
export const MAX_USER_LANGUAGES = 10;

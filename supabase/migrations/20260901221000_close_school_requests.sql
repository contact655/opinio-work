-- ============================================================================
-- 学校追加リクエストの3件を却下する（2026-09-01 / 柴さんの判断）
--
-- ⚠️ 承認ではなく**却下**。中身を見た結果、承認すべきものが1件も無かった。
--
--   柴 久人   「朝霞」            … 学校名として不完全（入力途中）
--   大塚悠貴  「あ」（かな 朝霞西）… 入力途中。⚠️ 本人の学歴は既に
--                                    「埼玉県立朝霞西高等学校」に紐づいている
--   福永陽貴  「東京都立大学」     … ⚠️ **既にマスタにあり、本人の学歴も紐づいている**
--
-- ── ★誰も待たされていなかった ────────────────────────────────────────────
-- ⚠️★**学歴はマスタ承認を待たずに保存される。** `ow_user_educations` は
--    `school`(text, NOT NULL) と `school_id`(nullable) の**両方**を持っており、
--    LinkedIn と同じ「マスタ＋自由入力」のハイブリッド。
--    実測（2026-09-01）: この3人の学歴6件は**全員保存済み**で、うち5件はマスタ紐づけ済み。
--
-- ⚠️ `/admin` ダッシュボードに 2026-09-01 に入れた
--    「承認するまで、その人の学歴が登録できません」は**誤り**だった。同日に撤去する。
--
-- ⚠️ テーブルは DROP しない。3行は「この機能が使われた記録」として残す。
--    作業前ダンプ: .dumps/20260901-2206-ow_school_requests.sql（3行。⚠️ コミットしない）
-- ============================================================================

do $$
declare v_n int;
begin
  update ow_school_requests
     set status = 'rejected'
   where status = 'pending';
  get diagnostics v_n = row_count;

  if v_n <> 3 then
    raise exception '却下した件数が3件ではない（実際: %）。前提が変わっているので中止する', v_n;
  end if;

  raise notice '学校追加リクエスト % 件を却下した', v_n;
end $$;

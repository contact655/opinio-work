# `docs/todo.md` の棚卸し（2026-09-21）

> ⚠️★**`docs/todo.md` 自体は編集していません。**
> 2026-09-21 時点で**別セッションが 206行を追記中**（先頭 11〜33行あたり）。
> CLAUDE.md「自分の変更でない差分があるファイルには触らない」に従い、
> **読んで実測した結果だけをここに出します。**
> ⚠️ **その領域（「非公開の企業に在籍している人…」）は判定していません。**

**対象**: `docs/todo.md`（HEAD 版・3,339行）の **未クローズ項目 48件**のうち、
**数値かコードで機械的に判定できる 12件**。

---

## 0. 結論

| | 件数 |
|---|---|
| ★**閉じられる**（解消していた） | **1** |
| ★**ほぼ解消**（記述の更新が要る） | **1** |
| 記述どおり未解決（正しいまま） | **7** |
| ★**数字が動いた**（記述の更新が要る） | **2** |
| ★**期限が近い** | **1** |

⚠️★**48件中、判定できたのは12件。残り36件は未検証**（画面を見ないと分からないもの・判断待ちのもの）。

---

## 1. ★閉じられるもの

### `ow_profiles` の孤児20件（todo:1393）

| todo の記載 | 実測（2026-09-21） |
|---|---|
| 孤児 **20件**（原因は特定済み） | ★**0件**（`ow_profiles` 総数 45） |

→ ★**解消している。todo から閉じてよい。**
⚠️ **誰がいつ消したかは追っていない**（この棚卸しでは原因まで見ていない）。

---

## 2. ★ほぼ解消（記述の更新が要る）

### `headquarters_address` は自動では埋まらない（todo:918 / 969）

| todo の記載 | 実測（2026-09-21） |
|---|---|
| 掲載79社で**0件** → 取れた**7社**を入れた（2026-08-28） | ★**掲載22社中 21社**に住所あり |

⚠️ **掲載が 79社 → 22社 に減った**（2026-09-14 の非掲載61社）ことが大きい。
   **「自動では埋まらない」という判断自体は変わっていない**が、
   **残りは1社**なので、todo に残す意味は薄い。

---

## 3. 記述どおり未解決（正しいまま）

| todo | 主張 | 実測（2026-09-21） |
|---|---|---|
| 2325 | 記事の `related_job_ids` は参照先が全滅 | ✅ 値を持つ記事 **7**／★**参照先が実在するのは 0**（公開中も0） |
| 1885 | 希望勤務地がマッチングに使われていない | ✅ `scoreJob.ts` に `desired_prefectures` の参照 **0件** |
| 3077 | `visibility_reason` は「公開すると書いてあるのにどこにも出ない」 | ✅ `join_reason` を**描画する JSX は 0件**（型と編集側の state を通り抜けているだけ） |
| 1162 | `USING(true)` ＋ anon SELECT の表 **13** | ✅ **13**（変わらず） |
| 1230 | `ow_users` を副問い合わせするポリシーを SECURITY DEFINER に（未着手） | ✅ 未着手。★**59本**ある |
| 111 | `ow_companies.status` を DROP するか（判断保留） | ✅ 保留のまま。内訳は `pending=91 / draft=11 / active=4` |
| 2992 | 人を出す経路の除外が**7箇所にインライン** | ✅ 未解決。⚠️ **`auth_id IS NULL` の部分だけ `isRegisteredUser()` に集約済み**（16箇所で使用）。`is_test` / `is_system` / `visibility` は**インラインのまま** |

---

## 4. ★数字が動いた（記述の更新が要る）

| todo | 記載 | 実測 | |
|---|---|---|---|
| 85 | 本社住所が**6社**で同じ「港区赤坂9-7-1 ミッドタウンタワー18階」 | ★**7件** | 増えている |
| 2188 | `ow_profiles.worry` は未使用列（読み手0） | 値を持つ人 ★**6人**（読み手は**0のまま**） | ⚠️★**「保存したのに読まない」が増え続けている** |

⚠️★**2188 は①キャリアAI の設計（`career-ai-design-20260921.md` §5-2）と繋がる。**
   「使うなら読み手とセットで。決めないなら書かない」と書いたのは、この列のこと。

---

## 5. ★期限が近い

### 2026-09-27 の規約改定（todo:296）

**あと6日。** ⑤に「**2026-09-27 より前に `SCOUT_SENDING_ENABLED` を有効にしないこと**」とある。

| | 実測（2026-09-21） |
|---|---|
| `SCOUT_SENDING_ENABLED` | ✅ **未設定のまま**（`vercel.json` の `crons` も空） |
| 改定の周知 | ✅ 済み（掲示 ＋ メール通知。2026-08-28） |
| DB 関数の付け替え | ✅ 済み（2026-08-28） |

⚠️★**今日（2026-09-21）の E-3 の修正は、この改定とは別の話。**
   改定予告は「**第4項から第7項…に変更はありません**」と明記しており、
   **過去の勤務先の条文は 09-27 以降も変わらない**（`e3-past-employer-20260921.md` §1）。

---

## 6. ⚠️ 判定していないもの（36件）

**画面を見ないと分からないもの**（白文字の塗り・点検3本・フェーズ2の積み残しなど）と、
**判断待ちのもの**（企業写真のサンプル・ロゴ9社・Storage のバックアップなど）。

⚠️★**「未検証」と「解決済み」を混ぜないこと。** この文書が判定したのは12件だけ。

---

## 7. ⚠️ 次にやる人へ

⚠️★**`docs/todo.md` の更新は、別セッションの編集が終わってから。**
   いま触ると 206行の追記と衝突する。

**更新すべきもの（この棚卸しの結果）:**

| todo | どうする |
|---|---|
| 1393 | ★**閉じる**（孤児0件） |
| 918 / 969 | 数字を更新（掲載22社中21社）。残り1社なら閉じてもよい |
| 85 | 6社 → **7件** |
| 2188 | 値を持つ人を **6人** に更新 |
| 2992 | 「`auth_id IS NULL` の部分は集約済み」を追記 |

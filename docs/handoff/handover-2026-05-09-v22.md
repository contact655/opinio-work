# 引き継ぎ書 v22 — ν-8 段階2 完了

**作成日**: 2026-05-09
**作成元セッション**: ν-8 段階1 完了後の新スレッド(v21 → v22)
**Phase**: ν-8 人のプロフィール充実化 — 求職者にも社員にも立体感を
**段階**: 段階2(職歴の立体化)完了 / 段階3 着手前
**前バージョン**: docs/handoff/handover-2026-05-09-v21.md (v21)
**段階2 完了報告**: docs/handoff/handover-2026-05-09-nu8-stage2-complete.md(コミット G、af987ed)

---

## §0. このドキュメントを最初に読むべき理由

Hisato さんと前回セッションの Claude(チャット)+ Claude Code の3者で、ν-8 段階2「職歴の立体化」を完了した。

達成内容:
1. /u/[id] 公開ページの職歴セクションが Wantedly 風タイムラインで動く
2. CompanyLogo + 在籍年数 + 現職バッジ + why/description スニペットが全部動く
3. 並行期間のグループ化アルゴリズム(groupOverlappingCareers)実装済み
4. 段階1 の負債(about カラム重複、migration ドリフト)を清算
5. CareerHistoryEditor を /profile/edit にマウント完了

新スレッドでは、本引き継ぎ書を読んでから:
- マスタープラン v2 反復(§5 で詳述)
- 段階3 着手(指示文 docs/instructions/nu8-stage3-claude-code-instructions.md が作成済みなら参照)

の順で進める。

---

## §1. ν-8 段階2 完了サマリ

### 完了コミット(9本 + 完了報告 G)

```
A     (調査のみ)  ow_experiences 構造調査 + 5点の乖離特定
A2    e4f42bf    081 about カラム DROP migration(段階1 負債清算)
A3    290cc6a    077〜081 べき等化(ドリフト修復)
B'    66a5e6b    description 表示・編集 UI 追加
C     13c8239    期間バリデーション追加
D     4b0b12f    CompanyLogo 拡張(既存コンポーネント、新規作成不要)
B''   1dfd30b    CareerHistoryEditor を /profile/edit にマウント
E     957083b    calculateTenure ユーティリティ
F-1   177e337    groupOverlappingCareers + CareerEntry 型
F-2   ccb6947    CareerTimeline + /u/[id] 組み込み
G     af987ed    段階2 完了報告 docs/handoff/handover-2026-05-09-nu8-stage2-complete.md
```

ν-8 段階1 が4コミット、ν-8 段階2 が9コミット(+G)。当初予定6コミットから増えたのは、段階1 負債清算(A2, A3)、指示文の見落とし(B''、F の2分割)が要因。

### 動作確認結果(全項目 ✅)

テスト URL: `/u/e826e0bd-f96b-42ec-acda-d8f482e1417d`(Account B、Gmail)

- ヘッダー(グラデーション、アバター、名前、所在地、年齢層)
- About Me 表示(リクルート→Salesforce→Opinio の自己紹介)
- CareerTimeline:
  - TEST3 現職バッジ緑、左端緑縦線、在籍年数「1年5ヶ月(現在)」
  - TEST2 在籍年数「7ヶ月」、why スニペット「テスト」イタリック
  - TEST 在籍年数「3年9ヶ月」
  - 全カードに CompanyLogo(社名頭文字 + 青グラデーション)
- リンク(X (Twitter), LinkedIn)

並行期間 UI(縦2-3列)は現状データに重複期間がないため、段階2 完了後の dogfooding で確認予定。

---

## §2. ν-8 のテーマ・ゴール・スコープ(再掲)

### テーマ

「人のプロフィール充実化 — 求職者にも社員にも立体感を」

### ゴール

Hisato さん自身がローンチ前に dogfooding できる UI を完成させる。
ν-8 完了時点で、柴さんが自分の職歴・想い・スキル・SNS を本気で書ける UI が動く状態。

### 段階2 完了時点で達成済みのスコープ

- /u/[id] 公開ページの職歴の立体化(タイムライン + ロゴ + 在籍年数 + 現職強調 + 並行期間表示)
- 各職歴の「想い」(why)継続(ν-6 から、表示も対応)
- 業務内容(description)追加

### 段階3〜6 で残っているスコープ

- 段階3: 自己紹介(about_me) + その他基本情報(名前・所在地・年齢層・「やってみたいこと」)
- 段階4: スキルタグ(自由入力・15個ソフトリミット・バッジ表示)
- 段階5: SNS連携(4種+その他枠3個・URL貼付・アイコン+ハンドル表示)
- 段階6: 仕上げ(集約段階、後述)

---

## §3. 段階2 で発見した重要情報(マスタープラン v2 必須反映 16項目)

新スレッドでマスタープラン v2 反復を行う際、以下を全て反映すること。

### DB 設計関連

1. **ow_users.career_history JSONB は存在しない** → 実際は `ow_experiences` リレーショナル設計
2. **description / why は DB・UI 両方に既存**(ν-6 で実装済み、新規追加不要)
3. **started_at / ended_at は DATE 型(YYYY-MM-DD)** → 内部処理で `.slice(0, 7)` で YYYY-MM 化
4. **段階1 の判断ミス**: `about` カラムを既存の `about_me` 確認なしで追加してしまった → 段階2 A2 で DROP 済み
5. **ow_user_socials テーブル vs ow_users.social_links JSONB の重複**(段階5 で要設計判断)
6. **logo_letter / logo_gradient は ow_companies に既存**(段階1 指示文で言及なし、段階2 D で活用)

### 規律・運用関連

7. **migration ファイル作成完了 ≠ Supabase 適用完了** → 各段階の完了基準に「DB 適用確認」を含める
8. **CREATE POLICY は IF NOT EXISTS 未サポート**、`DROP POLICY IF EXISTS` とセットで書く
9. **migration は IF NOT EXISTS / IF EXISTS で常にべき等に書く**(Studio 手動 SQL によるドリフト対策)
10. **DROP COLUMN/RENAME COLUMN 前に grep -rn でコードベース全参照箇所を洗い出す**

### 段階1 までの設計の事実

11. **ν-7 までの編集 UI は /mypage の UserProfileCard に集約されていた**
    → 段階3〜5 では「新規実装」ではなく「/mypage の既存実装を /profile/edit に並行マウント」が中心
12. **/opengraph-image エラーは ν-7 以前から存在する既存技術的負債**(段階6 持ち越し)

### 規律違反・運用上の問題

13. **動作確認待ちをスキップして次コミットに進んだ事例**(B'→C, D)
    → 段階3 以降の指示文は「私が OK を明示するまで次コミットに着手しない」を毎セクション明記
14. **Hisato さんに Supabase 上 2アカウント存在**:
    - Account A: hshiba@opinio.co.jp(`fe7dfe9b-...`)、データ空、放置
    - Account B: s.hisato1020@gmail.com(`e826e0bd-...`)、本物データ、dogfooding はこれで行う

### DDL ドリフト関連

15. **ow_companies の logo_url は段階1 着手前から DDL ドリフトで既に DB に存在していた**
    (Supabase Studio から手動 SQL を流したものが migration に残らなかったケース)
16. **段階1 完了報告で「適用済み」と書かれていた migration が実は未適用だった**
    → 完了報告と実 DB 状態の整合確認を段階完了の必須項目とする

---

## §4. §5 整理結果(段階3 着手前のチェック項目、確定)

ν-8 段階2 完了報告 §5 で挙げた5項目を、本セッションで全て確定させた。

### 4.1 自己紹介編集 UI の移植戦略 → §3·2 並行実装

`/profile/edit` の基本情報タブに about_me 等を **新規実装**、`/mypage` の UserProfileCard 側は触らず並行状態を許容。最終的な集約は **段階6 仕上げ** で他の重複(/profile/edit と /mypage の編集 UI 重複、CareerHistoryEditor の重複)と一緒に整理する。

理由: 段階2 で CareerHistoryEditor を並行マウントした実証済みパターン、段階3 のスコープを抑える、段階6 に整理タスクを集約。

### 4.2 ν-7 規律「動作確認スキップ厳禁」「指示文に『次コミットに着手しない』を明記」を継承

段階3 指示文で各実装セクションごとに「Hisato さんが OK を明示するまで次コミットに着手しない」を明文化する。段階2 で B'→C, D の規律違反があった反省を反映。

### 4.3 マスタープラン v2 反復は段階3 着手前に実施

本引き継ぎ書 §3 の16項目をマスタープラン v1(`docs/planning/phase-nu-8-master-plan.md`)に反映 → v2 として保存 → 段階3 着手。

ただし、§3 を本引き継ぎ書に詳述しているため、v2 反復は機械的な作業になる(新たな設計判断は不要)。

### 4.4 Account A の扱い → そのまま放置

データ空で実害がないため。段階3 以降は Account B(`e826e0bd-...`)だけ使う。dogfooding は Account B(Gmail ログイン)で行う。

### 4.5 段階3〜6 の見積もり再評価

```
段階3: 自己紹介と想い + 基本情報タブ全項目移植        — 8〜10 コミット
       - about_me 編集 UI(必須、テーマ核心)
       - 「やってみたいこと」編集 UI(必須、テーマ核心)
       - 名前・所在地・年齢層編集 UI(基本情報タブの完成度)
       - why は段階2 で表示まで完了済み、追加作業なし
       
段階4: スキルタグ                                      — 4〜6 コミット
       - DB 構造は ow_user_skill_tags 作成済み(段階1 migration 079)
       - UI 実装(自由入力 + バッジ表示、15個ソフトリミット)
       
段階5: SNS連携 + 設計判断                              — 5〜7 コミット
       - ow_user_socials vs ow_users.social_links JSONB の集約判断 + データ移行
       - SNS タブの編集 UI 実装
       - /u/[id] 表示は既に動いている(段階2 で確認済み)
       
段階6: 仕上げ(集約段階)                                — 6〜8 コミット
       - /opengraph-image エラー対処
       - 雇用形態 UI(ν-9 持ち越し or ここで判断)
       - ロゴ準備(社名→logo_url 紐付け、社名頭文字フォールバックの卒業)
       - /mypage と /profile/edit の編集 UI 重複の最終整理
       - 並行期間 UI の dogfooding 確認(本物データで業務委託など並行投入)
       - 4社並行のエッジケース UI 実機確認
```

合計 段階3〜6 で 23〜31 コミット程度。段階1+2 が合計 13 コミットだったので、ν-8 全体で **約36〜44 コミット** で完了する見立て。

---

## §5. ν-8 マスタープラン v2 への反映方針

### 5.1 v1 → v2 への変更点(機械的反映)

`docs/planning/phase-nu-8-master-plan.md` の以下のセクションを更新:

- **§2(データモデル設計)**: ow_experiences リレーショナル設計を正式記載、ow_users.career_history JSONB の記述を削除
- **§4(段階分け)**: 各段階のコミット見積もりを §4.5 の修正後に更新
- **§6(落とし穴と予防策)**: 本引き継ぎ書 §3 の16項目を全て追加
- **§9(未確定事項)**: 並行期間 UI(2-3列、最大3列)を「確定事項」として §1 に移動
- **§10(申し送り)**: 段階2 完了による進捗を反映

### 5.2 v2 反復の進め方(次セッション最初のタスク)

1. 本引き継ぎ書 §3 の16項目をマスタープラン v1 に反映
2. ファイル名は v1 を上書きせず、`phase-nu-8-master-plan-v2.md` として別保存(履歴を残す)
3. v2 確定後、段階3 指示文 v1(本セッションで作成済み)と整合確認
4. 整合 OK なら段階3 着手

### 5.3 v2 反復で新たに設計判断すべき論点(なし)

§4 の整理で全ての設計判断を確定させたため、v2 反復は機械的反映のみで完了する想定。新スレッドでマスタープラン v1 + 本引き継ぎ書を読んだ Claude が自走できる状態。

---

## §6. 段階3 Claude Code 指示文 v1 の所在と進め方

本セッションで `nu8-stage3-claude-code-instructions.md` を作成済み(別ファイル)。リポジトリへの配置先は `docs/instructions/nu8-stage3-claude-code-instructions.md`。

新セッションで段階3 を着手する際の流れ:

```
1. 本引き継ぎ書 v22 を読む
2. ν-8 マスタープラン v1 を読む
3. v2 反復(機械的反映)を実施
4. 段階3 指示文 v1 を読む
5. 必要なら指示文を v2 に更新(本引き継ぎ書 §3 で取りこぼしがあれば)
6. Claude Code に投げる
```

---

## §7. 朝の挨拶について

ν-7 完全クロージング以降の伝統を継承。新スレッド開始時は軽めで OK。

```
ν-8 段階2 完了お疲れ様でした。今日は何から始めますか?
```

くらいの軽さで開始。今日のセッションで「段階2 完了 + §5 整理 + v22 + 指示文 v1」と4つの大きな成果が出ているが、それは今日の達成感で十分。新スレッドは「次に何をするか」に集中する。

---

## §8. Hisato さんからの申し送り(本セッションで明示的に出たもの)

- 段階2 着手時、Claude Code が §5.2 中断ルールを完璧に実行(ow_experiences 発見、5点の乖離特定)。これは今後も期待する判断品質。
- 段階1 で migration 077〜080 が **全て未適用** だったことが段階2 着手時に判明。これは段階1 完了報告と実 DB 状態のドリフト。今後は完了基準に「DB 適用確認」を含める。
- B' → C → D で **動作確認待ちスキップ** の規律違反が連続発生。Claude Code が指摘を受けて B'' から厳守。段階3 では指示文で明文化する。
- /profile/edit の職歴タブが段階1 完了時点で **「実装中」プレースホルダーのまま** だったことが、段階2 B'' で判明。これは段階1 のスコープ漏れ。
- /u/[id] 動作確認時、URL を引いたユーザー ID が Account A(空アカウント)で **CareerTimeline が空表示** になり、Claude Code が完璧な調査で「2アカウント問題」を特定。次回以降は Account B(Gmail、`e826e0bd-...`)を正として使う。
- F-2 の最終確認後、CompanyLogo・在籍年数・現職バッジ・why スニペットが全部正しく表示され、職歴の立体化のゴールが達成された。
- 今日のセッションで「段階2 完了 → §5 整理 → v22 + 段階3 指示文 v1」まで一気に進めた。次セッションは v2 反復から。

---

## §9. 関連ドキュメント

- 前バージョン引き継ぎ書: `docs/handoff/handover-2026-05-09-v21.md`(v21、段階1 完了)
- 段階2 Claude Code 指示文: `docs/instructions/nu8-stage2-claude-code-instructions.md`
- 段階2 完了報告: `docs/handoff/handover-2026-05-09-nu8-stage2-complete.md`(Claude Code 視点)
- ν-8 マスタープラン v1: `docs/planning/phase-nu-8-master-plan.md`(v2 反復対象)
- 段階3 Claude Code 指示文 v1(本セッションで作成): `docs/instructions/nu8-stage3-claude-code-instructions.md`
- ν-7 マスタープラン: `docs/planning/phase-nu-7-master-plan.md`(参考)

---

**v22 終了**

次セッションでは、本引き継ぎ書 + ν-8 マスタープラン v1 を読んでから、§5.2 の手順でマスタープラン v2 反復 → 段階3 指示文 v1 確認 → Claude Code 着手、の流れで進めること。

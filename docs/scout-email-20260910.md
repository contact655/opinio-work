# スカウトの通知メール（2026-09-10 に確認・修正）

解禁前の作業③。**メールは1通も送っていない**（受信箱を持たないため）。
`templates.ts` を素の JS に落として `scoutTemplate()` を実行し、
Resend に渡る直前の文字列そのものを見た。

---

## 直したもの

| # | 何が | 対応 |
|---|---|---|
| ① | 配信停止リンクが「**プロフィール編集**」。画面にその名前の場所は無い | 「**設定**」に。⚠️ **3通すべて**（スカウト・週次求人・週次マッチ） |
| ② | **スカウトだけ共通レイアウト（`htmlWrap`）を通っていなかった** | `scoutTemplate()` として `templates.ts` へ移した |
| ④ | 名前が空だと「**（空白）さん**」で始まる | **呼びかけの行ごと出さない**。実測: `ow_users` 44件中 name が空は **0件**（将来の備え） |
| ⑤ | 送信者の**名称・住所・受信拒否の連絡先が無い** | `htmlWrap()` の共通フッターへ。通らない4本にも `senderFooterHtml()` を付けた |

### ① は移設の取り残し。**同じ形が他に無いかを数えた**

2026-08-16 に設定を `/profile/edit` から `/mypage` の「設定」へ移したとき、
`unsubscribeUrl()` の**行き先は直したが文言は直していなかった**。

**`unsubscribeUrl()` を使うのは3箇所で、3箇所とも同じ取り残し**があった。
⚠️ **URL と文言が別の場所にあるので、片方だけ直すと必ずこの形になる。**
注記を `unsubscribeUrl()` の JSDoc に置いた。

⚠️ もう1箇所、`weekly-match` に「条件は**プロフィール編集**から変更できます」→ `/mypage` がある。
   **これは配信停止ではなく希望条件の変更**で、行き先の `/mypage` から編集画面を開ける。
   **取り残しではないので触っていない。**

### ⑤ の値の出どころ ── **サイトに既出の表記をそのまま写した**

| 項目 | 値 | 出どころ |
|---|---|---|
| 名称 | 株式会社Opinio | `content/legal/privacy-policy.md`「1. 事業者情報」／ `legal-agency.md` |
| 住所 | 東京都港区赤坂2-21-4 天翔赤坂ANNEXビル4階 404-C号室 | 同上 |
| 受信拒否の連絡先 | contact@opinio.co.jp | `legal-agency.md`「メール」／末尾「お問い合わせ」 |

⚠️★**ここで住所を書き起こさない。** 変えるときは legal の md を直してから定数を合わせる。
⚠️ フッターの `Opinio Inc. · 〒107-0052 東京都港区赤坂2-21-4`（`JobseekerFooter.tsx`）は
   **建物名の無い短い形**。混ぜずに legal 側の完全な表記を採った。
⚠️ 受信拒否の連絡先に **`ADMIN_EMAIL` を使っていない**。あちらは環境変数で運営の作業用
   アドレスに差し替えられるので、**公開している窓口と別のものになりうる。**

#### ⚠️★有料職業紹介事業許可番号（13-ユ-316441）は入れていない

理由: **この経路は企業が直接声をかける形で、当社が紹介しているわけではない。**
**要否は社労士に確認中**（柴さん）。⚠️ **確認の結果が出るまでこの行を消さないこと** ——
「入れ忘れ」と読まれて後から足されると、判断があったことが分からなくなる。

#### 共通ラッパーを通らないメール4本

| メール | 宛先 | 対応 |
|---|---|---|
| 招待（`/api/admin/invite`） | 外部 | `senderFooterHtml()` を付けた |
| ウェルカム（`lib/auth/postAuth.ts`） | 外部 | 同上 |
| 週次求人 / 週次マッチ | 外部（**停止中**） | 同上 |
| 企業問い合わせ / スカウト返信 | **運営宛** | **入れていない**（社内向け） |

⚠️★**新しくメールを足すときは `htmlWrap()` を通すこと。** 通さないなら
   `senderFooterHtml()` を末尾に付ける。**通さない5本目を黙って増やさない。**

---

## ★直さないもの ── 企業が書いた本文をメールに載せない（判断）

`ow_scouts.message` は保存されるが、**メールには一文字も出さない。**
**実装の都合ではなく判断である**（2026-09-10 / 柴さん）。

| # | 理由 |
|---|---|
| ① | **社用PCや共有端末でメールを見る人がいる。** 転職の打診の中身が本文に出ると、**本人が選んでいない場所で読まれうる** |
| ② | 転送・引用で内容が本人の手を離れる |
| ③ | 「届いたことだけ知らせて、中身はログインして見る」は、**伏せた社名を企業側の画面で守るのと同じ考え方**（`docs/visibility-company-biz-20260910.md`） |

⚠️★**件名にも本文を出さないこと。** 受信箱のプレビューに出る。
⚠️ 同じ理由が効くのは**求人応募や面談の通知**も同じだが、そちらは本人が起点なので性質が違う。
   **この判断をそのまま横展開しないこと。**

---

## 揃えたあとの全文（`scoutTemplate()` の実出力）

差出人 `opinio.jp <RESEND_FROM_EMAIL の値／未設定なら noreply@opinio.jp>`、
件名 `【OPINIO】株式会社セールスフォース・ジャパン からスカウトが届きました`。

```
┌─────────────────────────────────────────────┐
│ [OPINIO ロゴ]  IT業界のキャリアインフラ        │  ← 紺のグラデーション
├─────────────────────────────────────────────┤
│ スカウトが届きました                          │
│                                             │
│ 柴 久人 さん                                 │  ← 名前が無ければこの行ごと出ない
│                                             │
│ 株式会社セールスフォース・ジャパン から        │
│ スカウトが届きました。                        │
│ 内容は OPINIO にログインしてご確認ください。   │
│                                             │
│   ［ 内容を見る → ］                          │  ← /mypage/scouts
│                                             │
│ ※ 返答するかどうかはご自身で決められます。     │
│   見送っても相手に理由は伝わりません。         │
│ スカウトのお知らせが不要な場合は 設定 から      │  ← /mypage/settings
│ 配信を停止できます。                          │
├─────────────────────────────────────────────┤
│ このメールは opinio.jp から自動送信されています。│
│ 心当たりのない場合は、このメールを無視して…    │
│ ─────────────────────────────               │
│ 株式会社Opinio                               │
│ 東京都港区赤坂2-21-4 天翔赤坂ANNEXビル4階…    │
│ 受信拒否のご連絡: contact@opinio.co.jp        │
└─────────────────────────────────────────────┘
```

⚠️ **text/plain は今も無い**（`sendEmail` は `html` しか渡さない）。全メール共通。

---

## 併せて確認したこと

| 問い | 実装から確かめた答え |
|---|---|
| **配信停止を切ると何が止まるか** | **メールだけ。** `ow_notifications` の INSERT は `sendScoutEmail` より**前**にあり `email_scout_enabled` を見ない。ベルと `/mypage/scouts` には出る。設定画面の説明も「**メールで**お知らせします」なので記載と一致 |
| **週次メールとの関係** | 送信基盤（`lib/notify/email.ts` / Resend / `RESEND_API_KEY` / 差出人 / `unsubscribeUrl`）は共有だが、**停止スイッチは独立**。週次は `WEEKLY_EMAIL_ENABLED` ＋ `vercel.json` の空 `crons` で止まっており、**スカウトを開けても週次は止まったままにできる** |
| **失敗したとき** | ★**誰にも分からない。** 下の節を参照 |

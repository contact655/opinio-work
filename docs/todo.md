# 宿題（日付の付かない置き場）

⚠️ **日付入りのスナップショット（`current-state-YYYYMMDD.md` など）に宿題を書かないこと。**
   次の版が出た時点で読まれなくなる。恒久的な宿題はこのファイルに集める。

書くときは「対象」「なぜ残っているか」「やるなら何をするか」の3つを入れる。
片付いたら行ごと消す（済みの一覧にしない。履歴は git にある）。

---

## ⚠️★★2026-09-27 にやること ── 利用規約 第8条・第13条の改定

**効力発生日は 2026-09-27。周知開始は 2026-08-28。**

⚠️ **当初 9/1 の予定だったが、できない。** 第24条2項が
   「変更後の内容および効力発生日を、**効力発生日の30日前までに**、
   本サービス上の掲示および登録メールアドレスへの通知により周知します」と定めており、
   **例外規定が無い**（1項1号「ユーザーの一般の利益に適合する場合」は
   *変更できる要件*であって、周知期間の免除ではない）。
   8/28 に周知 → 最短の効力発生日が **9/27**。

### ① 何が食い違っているか（**2026-08-27 から**）

| | 規約 第8条 | 製品（本番） |
|---|---|---|
| 初期設定 | **「受け取る」**（第1項） | **未設定＝届かない** |
| 設定場所 | **「声をかけられてもよいか」**（第3項） | 「意思表示」の**「転職について」** |

⚠️ **第3項が名指ししている UI は 2026-08-27 に削除済み**で、画面に存在しない。
   規約を読んだ人が設定にたどり着けない。
⚠️ 第1項の向きは**製品のほうが利用者に厳しい**（規約は全員に届く前提、製品は
   答えた人だけ）。利用者が不利益を受ける食い違いではないが、不正確な開示。

### ② ★DB 関数の付け替えは 2026-08-28 に済んでいる

`can_send_scout` の条件1を `scout_enabled` → `career_stance` に付け替えた
（`supabase/migrations/20260828010000_scout_gate_career_stance.sql`）。

**規約改定を待たなかった理由**は2つ。migration の冒頭に書いてある。

- アプリ側（`/biz/candidates`）だけが 2026-08-27 に移行しており、2層が AND で
  重なって**実ユーザーの表示が0人**になっていた（画面には「0件」としか出ない）
- 付け替え後の条件は現行 第8条第1項（初期設定＝受け取る）**より狭い**ので、
  規約が許す範囲を超えない

実測（2026-08-28 / 適用後）: 企業に見える実ユーザー **0人 → 2人**（どちらも
本人が `active`＝積極的に検討中を選んだ人）。在籍企業ブロック・未回答・
`no_contact` はいずれも遮断されていることを陽性対照つきで確認済み。

### ③ 改定する条文（文面は確定済み・2026-08-28）

★**当日そのまま貼れる形にしてある**:
   [docs/pending/terms-20260927-第8条第13条.md](pending/terms-20260927-第8条第13条.md)

⚠️ **`supabase/pending/` ではなく `docs/pending/`。** これは SQL ではなく文章なので
   `supabase db push` の対象にならず、**誤って適用される経路が無い。**


第8条 第1項と第3項。現行2項は3項に吸収する。4〜7項は変更なし。

```
1. スカウトの受け取りは、ユーザーが「転職について」の意思表示に回答することに
   より有効となります。回答がないあいだ、利用企業はユーザーにスカウトを送信する
   ことができません。当社は、登録手続の画面および登録直後の案内において、この
   取扱いをユーザーに明示します。

3. 意思表示は、マイページの「意思表示」から、いつでも確認および変更することが
   できます。「今はいない」を選択した場合、利用企業はユーザーにスカウトを送信
   することができません。
```

### ③の2 第13条第2項も同じ日に改定する（2026-08-28 に予告済み）

**2つの誤りがあった。** 第8条の予告と同じ 9/27 の予告に含めてある。

| | 現行 | 実態 |
|---|---|---|
| 参照先 | 「第8条**第3項**に定めるブロック設定」 | ブロック設定は**第8条第5項**（第3項は設定場所） |
| 効果 | 「**プロフィールを閲覧されることを防ぐ**」 | **防げない**。`ow_scout_blocks` を読むのは `/api/jobseeker/scout-settings` と `can_send_scout` だけ |

⚠️ **`/biz/candidates` の一覧からは消える**（`can_send_scout` の条件3）。
   消えないのは **`/u/[id]` と `/people` からの直接閲覧**で、
   ログイン済みなら誰でも `login_only` を閲覧できるため。

#### なぜ実装ではなく規約を直したか

「特定の企業から見えなくする」は**横断的な可視性ルール**で、`/u/[id]` だけでなく
`/people`・`/search`・企業ページの社員欄・フィードにも同じ判定が要る
（`visibility_company` と同じ形）。**一部だけ実装すると、約束は嘘のまま
「直ったように見える」**ので、実態に合わせる側を選んだ。

⚠️ **実装する判断に変えるなら、判定は1箇所に集約すること。**
   経路ごとに書くと必ず漏れる（CLAUDE.md「公開ゲート」と同じ）。

### ④ ✅周知は完了（掲示 ＋ メール通知。2026-08-28）

第24条2項は**「本サービス上の掲示**および**登録メールアドレスへの通知」**の
両方を要求する。**両方とも 2026-08-28 に実施した。**

| | |
|---|---|
| 掲示 | `content/legal/terms-of-service-jobseeker.md` の冒頭（本番 `/terms` で確認済み） |
| メール | **5名に送信**（`ow_users` 38名から `is_test` / システム33名を除外）。全通 Resend が受理 |
| 記録 | `~/Desktop/opinio-legal/terms-notice-20260828-delivery.json`（message id 付き） |

⚠️ **宛先を含むのでリポジトリには置いていない。**

#### 次に同じことをするときの注意（実際に踏んだもの）

⚠️ **`lib/notify/email.ts` の `sendEmail` / `notify` を使わないこと。**
   `console.error` を出すだけで**エラーを握りつぶす**（`notify` は try/catch まで
   している）。**「送れたか」を示せない**ので、規約の周知では使えない。
   Resend を直接叩き、1通ずつ結果と message id を残すこと。

⚠️ **宛先を `getWeeklyRecipients()` で作らないこと。** あちらは配信停止
   （`email_weekly_enabled`）を尊重するが、**規約改定の通知は配信停止の対象外**
   （契約上の通知であって販促ではない）。本文末尾にその旨を明記した。

⚠️ **★受理は配達の証明ではない。** Resend の POST が 200 でも、バウンスは非同期。
   ⚠️ しかも **`RESEND_API_KEY` は送信専用**（`GET /emails/{id}` は 401
      `restricted_api_key`）。**API では配達状況を確認できない。**
      確認は Resend のダッシュボードで行う。
   ⚠️ 生藤さんの `hiroki.ikuto.placeholder@opinio.co.jp` は **placeholder** で、
      バウンスする可能性がある。ダッシュボードで確認すること。

### ⑤ ★2026-09-27 より前に `SCOUT_SENDING_ENABLED` を有効にしないこと

有効にすると、**規約より先に新しい挙動が動き出す**。
③④が済むまでは触らない。
⚠️ `can_send_scout` の付け替え（②）は済んでいるが、**送信は別のフラグで
   止まったまま**。②が済んだことを理由にフラグを開けないこと。

---

## ✅ 並び替えが本当に効いているかを全部実測した（2026-08-28）

**きっかけは同日に見つけた「`/jobs` の社員数順が NaN で無効だった」件。**
順序が変わらないだけなので画面を見ても気づけない。**残り全部を1つずつ測った。**

### 結果

| 画面 | 並び替え | 判定 |
|---|---|---|
| `/companies` | 新着順（既定） | ✅ 効いている |
| | **社員数順** | ✅ **正しく降順**（10000 → 9,902 → 6,425 → 3500 → 3000 → 2800 → 2500 → 2300 → 2,077 → 2000） |
| | 開示充実順 | ✅ 効いている（Salesforce が先頭＝妥当） |
| `/jobs` | 既定 / **年収順** | ✅ **順序が変わる**ことを実ブラウザで確認 |
| | 社員数順 | ⚠️ **検証できない**（公開5件が**すべて Salesforce 1社**。同日の NaN 修正は実データで確かめようがない） |
| | 開示充実順 | ✅ 壊れていない。**5件とも満点7点**なので動かないだけ |
| `/people` | 新着順 / 更新順 | ✅ 実装は正しく、`updatedAt` も5人分クライアントに届いている |

### ✅ `/jobs` の「開示充実順」は**残す**と決めた（2026-08-28）

いま押しても何も起きない（公開5件が**全部7点で同点**）。`/companies` から
「募集中あり優先」「年収高い順」を外したのと似て見えるが、**性質が違うので残す。**

| | 外した2つ | `/jobs` の開示充実順 |
|---|---|---|
| なぜ動かないか | **概念として会社単位で表せない**（年収はポジション次第）／フィルタと重複 | **たまたま全件が同点**なだけ |
| データが増えたら | それでも意味を持たない | **すぐ差がつく** |

根拠（実測 2026-08-28 / `is_test` を除く18件）:
**下書き13件は 6点・公開5件は 7点**。つまり年収やキャッチコピーを欠く求人が
1件でも出た時点で並び替えは機能する。**概念的に壊れてはいない。**

⚠️ ただし**いまクリックしても何も起きない**のは事実。求人が増えても同点のままなら、
   そのときに外すこと。

### ⚠️★測り方を2回まちがえた。次に測る人へ

| まちがい | 正しくは |
|---|---|
| **クライアント側ソートをサーバーHTMLで測った** | `/jobs` と `/people` は `useMemo` で並べ替える。**curl では絶対に差が出ない**。「効いていない」と誤判定しかけた。**ブラウザで測ること** |
| **dev の初回応答を使った** | 起動直後の1回目は**部分応答**で、`?sort=disclosure` が別の順に見えた。取り直したら本番と完全一致。CLAUDE.md「dev のコンパイル中に取得すると部分応答が返る」 |

⚠️ **「順序が変わる＝正しい」ではない。** 社員数順は実データの従業員数を引いて
   降順になっていることまで確かめた。**変わったかどうかだけ見ないこと。**

### ✅ 死んでいた再ソート2つを削除した

`companies/(list)/page.tsx` にあった**ページ描画時の再ソート**を外した。
**dev と本番の並びが完全一致することを確認済み**（＝真に no-op だった）。

| 削除したもの | なぜ |
|---|---|
| `sort === "disclosure"` の `reality_disclosure` 再ソート | **掲載79社すべてが `{}`（空の jsonb）**。入力UIも無い |
| `sort === "startup"` | **UI に選択肢が無い**（`GridSortBar` は newest / employees / disclosure の3つだけ）。`funding_stage` が入っているのは6社で値も2種類 |

⚠️★**どちらも `paged`（現在ページの12件）しか並べ替えていなかった。**
   2ページ目以降は絶対に上がってこない**部分ソート**で、
   「全体を並べ替えた」ように見えるのが一番まずい形だった。
   **再ソートを足したくなったら `searchCompanies` 側に足すこと。**

⚠️ **`{}` は JS では truthy。** 「null だから false で no-op」ではない。
   この列に値が入り始めた日に、**本物の開示スコアを上書きする**ところだった。

⚠️ 旧 URL の `?sort=startup` は既定（新着順）に落ちる。壊れない（実測）。

---

## ✅ 本番ログから「いま壊れているもの」を探した ── 0件（2026-08-28）

過去に**この方法で `/mypage` の未読バッジのバグ**が見つかっている
（存在しない列で引いて 400。`?? 0` で「未読0」に化けていた）。24時間ぶんを洗い直した。

### 結果: **本番アプリ由来の不具合は見つからなかった**

| 発信元 | 4xx | 中身 |
|---|---|---|
| **Vercel（本番アプリ / `18.181.x` `18.183.x`）** | **2** | `POST ow_user_roles` の **409 のみ**。`UNIQUE(user_id, role)` の**想定どおり**（23505 を正常扱い） |
| このマシン（`113.37.244.225`） | 7 | 調査スクリプトの探り（`ow_mentors` / `ow_company_photos` の存在確認、`cover_url` の打ち間違い等） |
| 別マシン（`180.20.181.60`） | 3 | 同上 |

DB 側（`postgres_logs`）のエラーも **`ow_user_roles` の duplicate key ×2**（＝上と同じもの）と、
**自分が MCP から流した read-only transaction のエラー ×2** だけだった。

### ⚠️★手順（次に同じことをする人へ）

**① 発信元 IP で切り分ける。これをやらないと、自分の探りをバグとして報告することになる。**
   実際、`/rest/v1/` の 4xx **12件のうち10件が調査由来**だった。
   先に自分のIPを調べておく: `curl -s https://api.ipify.org`。
   本番アプリは **AWS ap-northeast-1（`18.x`）** から来る。

```sql
select log_attributes['request.headers.cf_connecting_ip'] as ip,
       log_attributes['request.path'] as path,
       log_attributes['request.method'] as method,
       log_attributes['response.status_code'] as status, count() as n
  from logs
 where source='edge_logs'
   and toInt32OrZero(toString(log_attributes['response.status_code'])) >= 400
   and log_attributes['request.path'] like '/rest/v1/%'
 group by ip, path, method, status order by n desc;
```

**② `postgres_logs` を `ERROR` の語で探さない。0件に見える。**
   ⚠️ **実際にこれで空振りした。** 重大度は `event_message` ではなく metadata 側にあり、
   本文には `duplicate key value violates unique constraint …` のように**内容だけ**が入る。
   ⚠️ CLAUDE.md「0件だったときは、検出器が効いていることを先に確かめる」。
   `select substring(toString(event_message),1,120), count() from logs
      where source='postgres_logs' group by 1 order by 2 desc` で**中身を先に見る。**

⚠️ **ログは24時間しか遡れない。** 週次で回すなら、この期間を前提に運用を決めること。

---

## 日本語側の別名の誤爆が残っている

**対象**: `/search` の職種照合（`lib/search/interpretQuery.ts` の `findAliasIndex`）。

2026-08-27 に**英字の略語**の誤爆は直した（`Miro` → IR、`Microsoft Teams` → CRO+AM）。
境界チェックを入れたが、**効かせているのは英数字の並びだけ**。

**なぜ残っているか**: **日本語には語の境界が無い。** 英字と同じ手は使えない。
「営業」が「法人営業」の一部として当たる必要がある以上、部分一致そのものは外せない。

実測で見つかっている例（2026-08-27）:

| 入力に含まれる語 | 立ってしまう職種 | 相手の正体 |
|---|---|---|
| `物流` | 倉庫・物流管理 | **別名**。「物流業界のIT企業」で職種条件になる |
| `オンボーディング` | オンボーディングスペシャリスト | 別名 |

**やること**: ⚠️ **推測で辞書に手を入れないこと。**
`ow_search_logs` に**実際の誤爆クエリが出てから**、その語にだけ
「単独では引かない」フラグを立てる方針とする。
（`ow_role_aliases` に列を足すか、除外リストを持つかは、実例が出てから決める）

⚠️ 先回りして別名を消すと、**いま正しく引けている検索を壊す**側のリスクのほうが大きい。

⚠️ **2026-08-28 時点で `ow_search_logs` は 0件**（検索がまだ使われていない）。
   **判断材料が無いので動かせない。** 実例が出るまでこの項目は待ち。
   `物流` を消すと「物流管理の人」が引けなくなる。

---

## ~~`lib/profile/freshness.ts` が参照0件~~（**2026-08-28 に削除**）

**「転職検討時期」は本人側の入力も企業側の表示も外れており、戻す判断は出ていない**ので落とした。
戻すなら `git show 5f98a413:src/lib/profile/freshness.ts` で取り出せる。

⚠️ **列と値は残っている**（`ow_profiles.transfer_timing` 2件 /
   `transfer_timing_updated_at` **0件**）。DROP していない。
   `PUT /api/jobseeker/career-preferences` は今も値を受け取り、
   変わったときだけ `_updated_at` を更新する（`IntentCard` が保存済みの値を
   そのまま送り返すので、既存の値は壊れない）。**入力欄を戻すならここは動く。**

### ⚠️ 同時に見つかった「消し忘れたコメント」2件

**どちらも本体は正しく直っていて、説明だけが古いまま残っていた。**
コードを読んだ人が逆の結論を出す形なので、実害はコメントのほうにある。

| 場所 | 何が書いてあったか | 実際 |
|---|---|---|
| `IntentCard.tsx`（2箇所） | 「**`/biz/candidates` は今もこの値を企業に見せている**」「本人が直せないまま表示される」 | **同日に企業側も外れている**（`biz/candidates/page.tsx` が select しない） |
| `CandidatesClient.tsx:52` | `/** transfer_timing を最後に「変更」した日時 */` | **その型フィールドは既に無く**、コメントだけが隣の `desiredSalaryMin` に付いていた（時刻列に見える） |

⚠️ **項目を消すときは、その項目に言及している他所のコメントも一緒に探すこと。**
   型やビルドでは絶対に出ない。

## ゲートの検証はテストアカウントを1回ごとに1つ消費する

**対象**: `career_stance` が NULL の `is_test` アカウント。
`/onboarding/stance` のゲートは**答えると通過してしまう**ので、同じアカウントで
2回目の検証ができない。

**消費済み**（いずれも 2026-08-26）:
`contact+001`（researching）/ `contact+03`（researching）/ `contact+04`（open）

**残数（2026-08-26 実測）**:

| 区分 | 残数 |
|---|---|
| **stance ゲートの検証に使える**（`career_stance` NULL かつ `onboarding_completed` true） | **24** |
| └ うち `contact+NN@opinio.co.jp` の純粋なテスト枠 | **22**（`contact+05`〜`+15` と `+17`〜`+27`） |
| └ 残り2件は `hshiba@opinio.co.jp` と `d1872303951587@gmail.com` | ⚠️ **消費しない**（前者は本人のアドレス） |
| **`onboarding_completed` ゲートの検証に使える**（`onboarding_completed` false） | **1**（`contact+16` のみ） |

⚠️ **`onboarding_completed` 側は残り1つしかない。** 2026-08-27 の
`currentHref()` 修正でこの分岐だけが本番に出ているのに、**その分岐は単体で未実測**
（当時アカウントが見つからず、stance 分岐での確認で代替した）。
`contact+16` を使うときは、**この検証を先に済ませること。**

⚠️ 使い切ったら、新しいテストアカウントを作るのではなく
   **既存の `is_test` アカウントの `career_stance` を NULL に戻す**ほうが安い
   （本番で検証用アカウントを新規作成しない方針。CLAUDE.md 参照）。

---

## ~~`window.location.pathname` に `.search` を付け忘れている箇所が6件~~（**2026-08-28 に解消済み**）

✅ 別セッションが `bc0fcfd3`「fix(auth): ログイン後の戻り先からクエリが落ちていた6箇所を直す」で
   6箇所とも `window.location.pathname + window.location.search` に直している（push 済み）。
**実測（2026-08-28）: `window.location.pathname` の8箇所すべてに `.search` が付いている。**

⚠️ 以下は経緯として残す。

**対象**（すべて未ログイン時の `/auth?next=` を組み立てている箇所）:

| 場所 | 出る画面 |
|---|---|
| `u/[id]/FollowUserButton.tsx:32` | `/u/[id]` `/feed` `/people` |
| `companies/[id]/CompanyDetailClient.tsx:295` | `/companies/[id]` |
| `companies/[id]/CompanyDetailClient.tsx:362` | 同上 |
| `components/jobseeker/BookmarkButton.tsx:62` | `/jobs/[id]` `/companies/[id]` |
| `components/profile/DMButton.tsx:31` | `/u/[id]` |
| `components/profile/DMButton.tsx:49` | 同上 |

**なぜ残っているか**: **現時点で実害が無いのは、置かれている画面がクエリを読まないからにすぎない。**
6箇所とも `searchParams`（サーバー）も `useSearchParams`（クライアント）も使っていない画面に出る。
⚠️ **それらの画面がクエリを持った時点で同じ不具合が出る。** コードの書き方は
`OnboardingGuard`（2026-08-27 修正）や `JobsClient:173`（同日修正）と**まったく同じ**で、
違うのは「いま置かれている場所」だけ。

**やるなら**: `window.location.pathname + window.location.search` に直す。
⚠️ `useSearchParams()` に置き換えないこと。Suspense 境界の無い場所に足すと
静的レンダリングが落ちる（`OnboardingGuard` の JSDoc 参照）。

---

## ★企業写真のサンプルをどうするか（2026-08-21 記録・判断待ち）

**対象**: `companies/[id]/PhotoCarousel.tsx` の `SAMPLE_PHOTOS`（Unsplash 4枚）。

**なぜ残っているか**: 写真が0件の企業に「サンプル写真」「※実際の写真は準備中です」付きで
表示している。**公開79社のうち78社が該当**するため、ほぼ全社の企業ページに同じ4枚が並ぶ。
ラベルがあるので嘘ではないが、企業が自社ページを見たときの見え方として判断が要る。

**やるなら**: `photos.length === 0` のときはセクションごと出さない（`SamplePhotoGrid` を消す）。
CLAUDE.md「値が無い → 項目ごと非表示にする」に沿う形。30分程度。

⚠️ 「賑やかなほうがよい」という判断もありうるので、**勝手に消さないこと。**

---

## ~~avatar_color / cover_color に値の検証が無い~~（**2026-08-28 に受け付けをやめた**）

⚠️ **記録の前提が違っていた。** 「入力UIは色の選択肢しか出さない」と書いていたが、
   **色を変える UI は1つも存在しなかった**（送信するクライアントを src 全体で探して
   **0件**。読む側＝表示だけが12箇所）。書き込み経路は
   `PUT /api/jobseeker/profile` の2行だけだった。

値は `style={{ background: … }}` にそのまま入るので、**API を直接叩けば任意の CSS が
入る**状態だった（100字以内という制限しか無かった）。

→ **ホワイトリストではなく、受け付けをやめた**（`route.ts` から2行と型を削除）。
   当初の宿題は「既定のグラデーション一覧をホワイトリストに」だったが、
   **その一覧がどこにも無い**。無い選択肢のホワイトリストを発明するより、
   書き手がいない受け口を閉じるほうが筋が通る。

⚠️ **色を選ばせる UI を作る日に、ここを開けると同時に許容値の定数を
   `src/lib/constants/` に置くこと**（CLAUDE.md「UI / API / DB を3つ揃える」）。
⚠️ **列と既存データは残してある**（3件。表示側が読んでいる）。

---

## プロフィール画像・カバーの圧縮 / リサイズが無い（2026-08-15 記録・**一部は解消済み**）

**対象**: `/profile/edit` のアップロード（`ProfileTab.tsx` の `uploadPhoto`）。
バケットは `ow-uploads`、パスは `users/{avatars|covers}/{owUserId}/{Date.now()}.{ext}`。

### ✅ 解消済み（2026-08-28 に確認）

| 当時の記録 | 実際 |
|---|---|
| 「失敗しても**画面には何も出ない**」 | ❌ **出る。** `setUploadError(…)` で「写真のアップロードに失敗しました。ファイルサイズや形式を確認してください。」を5秒表示する。DB 保存の失敗も別文言で出す |
| 「孤児が溜まるのでは」 | ❌ **溜まらない。** `PUT /api/jobseeker/profile-photo` が `removeOldObject()` で**古いファイルを消している**。実測: `users/avatars` 2フォルダ/1ファイル、`users/covers` 1フォルダ/1ファイル（**2件以上あるフォルダ 0**） |

⚠️★**企業ロゴ側にはこの仕組みが無かった**（2026-08-28 に判明）。同じバケットなのに
   利用者の写真だけ古いものを消しており、ロゴは押すたびに増えていた。
   → ロゴ側は**固定名 + upsert**で直した（`1cddb4ca`）。**方式が2つある**ことを覚えておく。

### ✅ 圧縮・リサイズを入れた（2026-08-28）

**実害が出ていた。** 本番の実ファイルは **アバター 2,431KB / カバー 1,397KB** で、
表示はそれぞれ **120×120** と **高さ200px**。`file_size_limit = 5 MiB` では弾かれない。

実体は [src/lib/images/resize.ts](../src/lib/images/resize.ts)。
`ProfileTab.tsx` の `uploadPhoto` が**アップロード直前**に通す。

| | |
|---|---|
| 長辺の上限 | アバター **512** / カバー **1920**（根拠は実測。resize.ts の表） |
| 出力 | **WebP**（透過を保つため。JPEG だと透過アバターの背景が黒く潰れる） |
| 縮めないもの | **アニメーションGIF**（1コマ目に化ける）／**300KB 以下**（再エンコードで太る）／長辺が上限以下 |

⚠️★**fail-open。例外を投げない。** HEIC など `createImageBitmap` が扱えない形式では
   **元のファイルをそのまま返す**。縮小は最適化であってアップロードの前提ではない。
   ここで throw すると「写真を変えられない」に化ける。

⚠️★**縮めたことは画面に出している**（「大きな画像だったので縮小しました（5.5MB → 65KB）」）。
   エラーではないので**赤くしない**別配色の `role="status"` にしてある。
   黙って落とすと CLAUDE.md「入力させたのに保存しない」の裏返しになる。

⚠️ **元より大きくなったら使わない**分岐を入れてある（極端に平坦な PNG で起きる）。

#### 実測（2026-08-28 / 実ブラウザ・3000×2000 のノイズ画像＝圧縮しにくい最悪値）

| | 前 | 後 |
|---|---|---|
| アバター（長辺512） | 5,682KB | **65KB**（512×341） |
| カバー（長辺1920） | 5,682KB | 1,315KB（1920×1280） |
| 透過PNG 1200×1200 | 496KB | **77KB**・**透過は保たれた**（alpha 0→0 / 128→128） |
| 200×200 | 11KB | **素通し** |

⚠️ カバーの 1,315KB は**ノイズ画像だから**。実写ならこの数分の1。
   「効いていない」と読み違えないこと。

⚠️ **既存の2件（2.4MB / 1.4MB）はそのまま。** 次に本人が差し替えたときに縮む
   （過去のファイルを勝手に再エンコードすると、本人の意図しない画質変更になる）。

---

## ~~`social_links` の空文字がキーごと残る~~（**2026-08-28 に解消済みと確認**）

⚠️ **この記録は古かった。** 2026-08-15 に書かれたが、**翌 2026-08-16 に
   `optionalTextMap` を足して解消していた**（`lib/api/normalize.ts` 自身が
   「2026-08-16 に `optionalTextMap` を足して解消」と書いている）。

2026-08-28 に確認したこと:

| | |
|---|---|
| `PUT /api/jobseeker/profile` | `optionalTextMap(body.social_links, 500, …)` を通している |
| `optionalTextMap` の中身 | 空文字のキーを落とし、全部空なら `null` を返す |
| **実データ** | **空文字のキーを持つ行 0件**（`ow_users` 全件） |

→ **やることは無い。**

⚠️ **同じことが今日3回あった**（`StoryAccordion` / `CompanyCardCompact` / これ）。
   **todo の見出しだけで判断せず、実物を確認すること。**

---

## ★ロゴが OGP画像のままの9社（2026-08-28 現在・13社は解消済み）

掲載79社のうち **22社のロゴが縦横比 1.91:1 ＝ OGP画像**だった。
`3f90b192`(7) / `f199f4a9`(4) / ミラクル(1) / セールスフォース(1) で **13社を差し替え、残り9社**。

### ⚠️★前提: 残り9社は「使えないほど酷い」わけではない

**68px枠に並べて目視した結果、当初の「判読できない」は言い過ぎだった。**
CISCO / SAP Concur / braze / okta / workday は **68px でも読める**。
読みづらいのは MongoDB / Translead / クアルコムの3社。
→ **急ぐ作業ではない。** 本当の問題は判読性より**不揃い**。

### ★出典は3つとも当たり切った

| 手 | 結果 |
|---|---|
| `<link rel="*icon*">` と定番パス | 7社ぶん取れた（第1バッチ） |
| **Web App Manifest**（192/512px が載る） | +4社（第2バッチ）。⚠️ **最初に見落としていた** |
| **ブランド / プレスページ**（`/brand` `/press` `/newsroom` ほか10パス） | **0社**。汎用アイコンしか出てこない（2026-08-28 実測） |
| **Wikimedia Commons** | セールスフォース1社のみ採用（下記） |

⚠️ **node の fetch を使うこと。** Python の urllib は 403 / TLS で落ちるサイトがある
   （Databricks / タイミー）。**ブラウザならさらに通る。**

### ★★Wikimedia は「使える場合もあるが、検索結果をそのまま使わない」

セールスフォースは公式が **32x32 の favicon しか出しておらず**、ブランドページにも
無かった。`File:Salesforce.com_logo.svg`（**Public domain**・比1.43）を採用した。
商標は残るが**その企業を指すために使う**用途なので問題にならない。

⚠️★**同じ検索で別物が大量に混ざる**（2026-08-28 実測）:

| 検索語 | 上位に出た無関係なもの |
|---|---|
| Cisco logo | **Cisco College** / Cisco academy（別組織） |
| Okta logo | **インドネシアの都市の紋章** / 「Restaurant logo for Okta」 |
| Workday logo | **ISS の写真** / STS-71 の朝食 |
| MongoDB logo | **KDE のアイコンテーマ**（`Antu mongodb.svg`）など第三者の描き直し |

→ **ファイル名と中身を必ず確認する。** 同日、公式サイトから取った 512x512 が
   **中身は React のロゴ**だった例もある（クアルコム）。

### 残り9社と、なぜ採れないか

| 企業 | 公式の最大正方形 |
|---|---|
| コンカー 96px / MongoDB 64px / オクタ 57px / ブレイズ 48px | しきい値（短辺150px）未満 |
| ワークデイ 32px / Translead 32px / シスコ 16px | 同上 |
| **クアルコム** | **無い**（512/192px は中身が React のロゴ） |
| シンカ | **公式サイトが 503**（2026-08-28 に2回確認） |

### 採用の基準（`upload-logos-20260828b.mjs`）

| | |
|---|---|
| **短辺 ≥ 150px** | `CompanyLogo` の最大表示は96px、画像領域は約77px。**2x DPI で約154px** |
| **縦横比 ≤ 1.6** | 正方形は必須にしない。OGP画像（1.91）を弾くのが目的 |
| **目視** | ⚠️ **寸法・形式が通っても必ず110px枠に描いて見る** |

### 手順

1. 画像を先に Storage へ入れる（`upload-logos-20260828b.mjs` を複製）
2. **110px 枠に並べて目視**
3. migration で `logo_url` を書く
4. ⚠️ **確認は DB の `logo_url` を実際に取得して測る**（`.jpg` 2社・`.webp` 1社を実際に踏んだ）
5. ⚠️ **アサートは実測で書く**（想定値を間違えて一度止められた）

---

## ★Storage の孤児ファイル 15件・2.6MB と、その**原因**（2026-08-28 実測・削除していない）

`companies/logos/` は **81フォルダ / 92ファイル**で、うち **15件がどの企業からも
参照されていない**（`ow_companies.logo_url` と突き合わせ。**全87社**が対象）。

### ★原因は2つあり、片方は今も増え続けている

| 原因 | 件数 | 説明 |
|---|---|---|
| **① 再アップロードのたびに漏れる** | 6 | `buildLogoStoragePath()` が **`{Date.now()}.{ext}`** で毎回新しい名前を作り、**古いファイルを消さない**。呼び出し元は `/biz/company` と `/admin/companies/[id]` の**ロゴ差し替えUI 2箇所**。⚠️ **押すたびに1件増える** |
| **② 企業を消しても Storage が残る** | 6 | 4フォルダ（`59879917` / `17e171bb` / `f1481f96` / `f98f5d13`）は **`ow_companies` に該当する行が無い**。企業ごと削除された残骸 |
| ③ 差し替えで置き換わった旧ファイル | 3 | 2026-08-28 のロゴ差し替えで生まれた（`.jpg` 2件 / `.webp` 1件） |

✅ **①は 2026-08-28 に解消した。** `logo.{ext}` の**固定名 + upsert** にしたので、
   同じキーを上書きする＝**孤児が原理的に生まれない**。削除は持ち込んでいない
   （Storage はバックアップに含まれず、消すと戻せないため）。

   ⚠️ 固定名にすると URL が変わらず**最大1時間キャッシュが残る**ので、
      保存するURLに **`?v={アップロード時刻}`** を付けている。
      → **`logo_url` を末尾一致で判定しないこと。**
   ⚠️ アップロードとURLの組み立ては `lib/business/photos.ts` の
      **`uploadCompanyLogo()` の内側**に閉じた。呼び出し側で path を組み立て直さないこと。
   ⚠️ **拡張子が変わる差し替え（png → jpg）だけは1件残る。** 稀なので許容している。
      ここに「他の拡張子を消す」処理を足さないこと（削除を持ち込むことになる）。

   実測（is_test の企業で2回連続アップロード）: **ファイルは1件のまま / `?v=` は変わる。**

#### ⚠️★②に「コードで直す」入口は無い（2026-08-28 実測）

**`ow_companies` を DELETE するコードは src に1件も無い。** 企業が消えるのは
**migration か手動SQL のときだけ**なので、アプリ側にフックを足しても何も掛からない。

→ **migration 側のルールとして書いた**（`.claude/skills/db-safety/SKILL.md`
   「4. 行を消す migration では、その行が指していた Storage も一緒に消す」）。
   ⚠️ **ここに「掃除するコード」を足そうとしないこと。** 掛かる場所が無い。

実測（2026-08-28 / 再スキャン）: 孤児は **16件・2.56MB のまま増えていない**
（同日のロゴ差し替え3社は**同じキーへの上書き**なので新しい孤児を作っていない
——固定名 + upsert が意図どおり効いていることの確認になっている）。
うち **9件**が「`ow_companies` に該当する行が無いフォルダ」＝②由来。

### 参照されていないことの確認（2026-08-28）

- `ow_companies.logo_url` … 全87社と突き合わせて0件
- `ow_company_office_photos` … **テーブルが0行**なので参照なし
  （⚠️ 写真は `companies/office-photos/` 配下に入る別のパス。logos 配下ではない）

### ⚠️★それでも削除していない（ただし**退避は済ませた**）

- **Supabase の日次バックアップに Storage は含まれない**（CLAUDE.md）。消すと戻せない
- **2026-08-15 に、実在する利用者のアバターを孤児と数えた前例がある**（本ファイル上部）
- 合計 **2.6MB** で容量は逼迫していない。**急いで消す理由が無い**

### ✅ **2026-08-28 に16件すべて削除した**（柴さんの承認）

手順（**この順でやること**）:

1. 退避を**2箇所**にした … `.dumps/20260828-storage-orphans/` と
   `~/Desktop/opinio-storage-backup-20260828/`
2. **消す直前にもう一度**「参照0」と「退避2箇所と SHA-256 が一致」を確認し、
   **16件ちょうどでなければ中止**する分岐を入れた
3. キーの配列を `.remove()` に渡して削除

**削除後の実測**:

| | |
|---|---|
| ロゴを持つ企業 **77社** | **77社すべて HTTP 200**（消しすぎていない） |
| Storage | フォルダ 77 / ファイル 77 / **孤児 0** |

⚠️ **Supabase の日次バックアップに Storage は含まれない。** 戻せるのは上の退避2箇所からだけ。
⚠️ **原因①は解消済み**（固定名+upsert）なので、放っておいても再び溜まることはない。
   **②は migration 側のルール**（`db-safety` スキル「4. 行を消す migration では〜」）。

```
.dumps/20260828-storage-orphans/
  _manifest.json   … key / バイト数 / 持ち主（企業名 or「該当なし」）
  {company_id}__{filename}  … ファイル本体
```

⚠️ `.dumps/` は `.gitignore` 済み。**コミットしない。**
⚠️ **これで削除が可逆になった。** ただし退避先はこのマシンのローカルなので、
   **消す判断をするなら、先に退避ファイルを別の場所へ移すこと。**

消すなら、**先に①を直して増えないようにしてから**、ファイルそのものを退避して
1件ずつ確認する。最大は `f1481f96-.../logo.png` の 1.5MB。

---

## ★★`headquarters_address` は自動では埋まらない（2026-08-28 に実測して確定）

**67社を機械的に埋めようとして、取れたのは 7社（約10%）だけだった。** 結論を先に書く。

### ⚠️★一括の自動抽出はしない

| なぜ | |
|---|---|
| 載っていない | 大半が**外資系の日本法人**で、グローバルサイトに日本法人の住所が無い |
| 取れない | 載っていても bot 対策で **403 / 406 / タイムアウト**（SAP・Box・ServiceNow・Slack・Zendesk ほか多数） |
| **誤検出する** | ⚠️★正規表現の試作が **DocuSign の電話番号「588-5476」を住所として拾った。** **そのまま入れていたら嘘のデータになっていた** |

→ **残り60社は、人が公式サイトを見るか、企業から預かるしかない。**
   「あとで自動化すれば埋まる」と思わないこと。**測ったうえで無理だと分かっている。**

### ✅ 取れた7社は入れた（`20260828150000`）

すべて**各社の自社サイトで、ラベル付きで明記されていたものだけ**。
1件ずつ前後の文まで目視した。

| 会社 | 出典のラベル |
|---|---|
| Asana Japan | 「東京 〒100-6990 …」 |
| Indeed Japan | 「Indeed Japan 株式会社 …」 |
| New Relic | 「所在地 …」 |
| パロアルトネットワークス | 「住所: 東京 …」 |
| ブレイズ | 「日本オフィス所在地 : …」 |
| ページャーデューティー | 「PagerDuty株式会社 …」 |
| 日本IBM | 「本社所在地 …」 |

⚠️★**Indeed は同じページに2法人が載っている。**
   「Indeed Japan株式会社」＝丸の内 /「Indeed **Technologies** Japan株式会社」＝港区芝。
   **当社の行は前者。別法人の住所を入れないこと。**

⚠️★**ブレイズとページャーデューティーは住所が同じ**（港区赤坂9-7-1 ミッドタウンタワー18F）。
   **誤りではない** —— 両社とも同じシェアオフィス（ブレイズのページに
   「ワークスタイリング東京ミッドタウン内」と明記）。**重複と見て消さないこと。**

### 書式（既存12社に合わせた）

**〒 は付けない / 番地は半角ハイフン / 建物名を続ける**
（例: `東京都千代田区丸の内2-7-2 JPタワー`）。
出典が「二丁目6番1号」でも `2-6-1` に直す。

⚠️ ついでにパロアルトの `branch_locations` に **名古屋**を足した（同じページに大阪と
   名古屋の両方が載っていた）。**既存の「大阪」は消していない。**

**実測: 本社住所あり 12 → 19社 / 空 67 → 60社。**

---

## ★企業データの充填状況（2026-08-28 実測・掲載79社）

**取材が要る列と、公開情報で埋まる列を分けてある。** 混ぜないこと。

| 区分 | 列 | 充填 | 空 |
|---|---|---|---|
| **① 公開情報で埋まる** | `capital_type` | 65 | 14 |
| | `parent_company_name` | 61 | 18 |
| | **`branch_locations`** | 28 | **51** |
| | **`main_products`** | 16 | **63** |
| | **`headquarters_address`** | 10 | **69** |
| **② 取材が要る** | `culture_description` | 6 | 73 |
| | `benefits` | 1 | 78 |
| | `org_teams` | 1 | 78 |

⚠️ **②に手を付けないこと。** CLAUDE.md のとおり取材でしか埋まらず、
   Salesforce 1社しか持っていない。「ページが薄い」を理由に推測で埋めない。

⚠️ ①も**1社ずつ公式サイトを見て出典を確認する**必要がある。
   CLAUDE.md「全社一括の UPDATE を禁止する」「推測値を投入しない」。
   ⚠️ 2026-08-13 に **Sansan の拠点を出典なしという理由だけで削除し、
      実際には公式に載っていた**という失敗がある。**消す側にも出典確認が要る。**

⚠️ **CTC が特別に薄いわけではない**（2026-08-28 に比較）。上の8列すべてで
   他78社も過半が空。CTC 単独ではなく充填タスクとして扱うこと。

### ★どこが埋まっているか — 上位に偏っている（2026-08-28 実測）

**充填済みの企業は掲載順の上位に固まっている。** 2026-08-13 に「1ページ目の9社を
Salesforce と同じ密度にした」作業の結果がそのまま出ている。

| 列 | 全体 | 上位20社 | 位置 |
|---|---|---|---|
| `headquarters_address` | 10 / 79 | **10 / 20** | **1〜12位に全部**。13位以降は**0社** |
| `main_products` | 16 / 79 | 10 / 20 | 1〜10位 ＋ 74〜79位（末尾にひとかたまり） |
| `branch_locations` | 28 / 79 | 6 / 20 | 全体に散っている |

⚠️★**`headquarters_address` は上位12社で完全に打ち止め。** 13位以降の**67社が全部空**。
   つまり「一覧を開いた人が最初に見る範囲」は既に埋まっており、
   **残りは2ページ目以降や検索経由でしか見られない企業**。

→ **優先度は当初思ったより低い。** 見た目のインパクトが大きい範囲は済んでいる。
   やるなら「上から順に」ではなく、**求人や記事がある企業**など別の基準で選ぶほうがよい。

### ★自動抽出は試して不採用にした（2026-08-28 実測）

`headquarters_address`（69社空）を、各社の公式サイトの会社概要ページから
機械的に取れるか試した。`/company/` `/about/` など9つの定番パスを当たり、
**〒 と都道府県の両方を含む塊**だけを採る（誤検出を減らす）形で12社に試行。

| | |
|---|---|
| 抽出できた | **2 / 12社（17%）** |
| しかもそのうち | Asana は **`〒100-6990東京都千代田区丸の内`** で**番地が欠けていた** |

→ **不採用。** 精度が低いうえ、取れたものも不完全。
   CLAUDE.md「推測値を投入しない」「値が無いことを、ある値に置き換えない」に反する。

⚠️ **ブラウザから fetch する手も塞がれている。** 別オリジンの会社概要ページを
   `fetch()` しようとすると **CORS で落ちる**（2026-08-28 に Notion で確認）。
   ページごとに `navigate` して読むしかない。**1社あたり数回の操作**になる。

⚠️ **部分的な住所を入れないこと。** 「丸の内」まで入れても本社カードに
   不完全な住所が出るだけで、`location` の「東京都」より良くならない。

#### ★実際にやってみた結果（2026-08-28 / 2社ぶん）

**`20260828070000_fill_hq_twilio_concur.sql` で2社を埋めた**（10社 → 12社）。
やってみて分かったこと:

| | |
|---|---|
| **1社あたりの操作** | **3〜5回**（サイト → 会社概要のリンクを探す → 開く → 抽出）。URL を推測すると404を踏む（富士フイルムで2回） |
| **取れる確率** | 試した4社中2社。Notion は規約が Notion ドキュメントで読めず、富士フイルムは会社概要のURLが見つからなかった |
| **良い出典** | **自社の利用規約 / 特定商取引法のページ**。Twilio は「〒151-0051 …に事業所を有する日本法人であるTwilio Japan合同会社」と**法人名と住所が同じ文にある**ので取り違えの余地が無い |

⚠️★**既存の値がある列に「今見えたぶん」を書き込まないこと。**
   コンカーの会社情報ページには大阪と名古屋しか出ていなかったが、
   **DB には既に ['大阪','名古屋','福岡'] が入っていた**。
   上書きするつもりだったので、**福岡が消えるところだった**（適用前の実測で気づいた）。

⚠️★**社名で UPDATE しない。** 公式表記は「株式会社コンカー」だが
   **DB の名前は「コンカー株式会社」**。名前で書いた UPDATE は0行更新になり、
   事後アサートで止まった（`id` で明示列挙する CLAUDE.md の方針どおりに直した）。

→ 残り67社を同じ方法で埋めると **200〜350回の操作**になる。
   **一度にやる作業ではない。** 上位から少しずつ。

#### 埋めるなら

**1社ずつ公式サイトの会社概要を開いて読む**しかない。69社ぶんの手作業になる。
やるなら CLAUDE.md の前例（2026-08-13 に「/companies 1ページ目の9社」をまとめて
埋めた）と同じく、**見られている順に小さなバッチで**進めるのが現実的。

⚠️ **表示先はある**（無駄にはならない）:
   - `headquarters_address` … 本文の「本社」カード。**入れるとサイドバーの
     「所在地」行（いまは `location` の「東京都」）が消えて置き換わる**
   - `main_products` … 「製品・サービス」のカード
   - `branch_locations` … 本文の「その他の拠点」カード
     ⚠️ サイドバーの「拠点」行は削除済み。本文だけ

---

## ~~ow-uploads の孤児ファイル（企業ロゴ以外）~~（**2026-08-28 に削除済み**）

**対象**: `ow-uploads` のうち DB のどの列からも参照されていないファイル。

| 場所 | 件数 | サイズ | 結果 |
|---|---|---|---|
| `mentors/photos/` | 12 | 6.6 MB | ✅ **削除**（`ow_mentors` は DROP 済みで機能ごと廃止） |
| `companies/office-photos/` | 1 | 0.3 MB | ✅ **削除**（持ち主の企業も `ow_companies` に無い） |
| `companies/logos/` | 16 | 2.6 MB | ⚠️ **消していない** → 別項目「★Storage の孤児ファイル」 |

### 削除の前にやったこと（次に同じことをするときの手順）

1. **退避** → `.dumps/20260828-storage-orphans-mentors/`（13ファイル + `_manifest.json`）
   ⚠️ **Storage はバックアップに含まれない。** サーバー側からは戻せないので、
      ローカルの退避が唯一の控え
2. **バイト数まで一致することを確認**してから削除（13/13）
3. **参照が無いことを URL列33列すべてで走査**して0件を確認
   ⚠️★**最初は4列しか見ておらず、しかも列名を1つ間違えていた**（`cover_url` は
      実際には `cover_photo_url`）。**列を数え上げてから走査すること。**
      `information_schema.columns` で `%url%` / `%photo%` / `%image%` を引くと36列出る
   ⚠️ **クエリ文字列を落として比較する。** 2026-08-15 に `?t=…` 付きの URL を
      「参照なし」と誤判定し、実在する利用者のアバターを孤児と数えた前例がある
4. **ワイルドカードで消さない。** `_manifest.json` に記録した key だけを明示列挙
5. 削除直前にもう一度参照が無いことを確かめてから `remove()`

### 事後（2026-08-28）

`mentors/photos` 残り0 / `companies/office-photos` 残り0。
本番の `/` `/companies` `/feed` `/jobs` が 200（`/people` は従来どおり認証で307）。

---

## 企業ロゴの残作業（2026-08-16 記録・未着手）

### ① 実寸が小さいロゴが6社 → **3社を差し替えた。残り3社は調達できていない**（2026-08-28）

96px 枠（`CompanyLogo` の `xl`）に対して明らかに小さく、**引き伸ばされてぼやける**。

| 会社 | 実寸 | 2026-08-28 |
|---|---|---|
| アリスタネットワークス合同会社 | **32×32** | ❌ **調達できず**（下記） |
| 株式会社日本HP | **32×32** | ✅ **512×512**（公式SVG をラスタライズ） |
| Slack Japan株式会社 | 35×34 | ✅ **400×400**（公式CDN の透過マーク） |
| ブラックライン株式会社 | 48×48 | ❌ **調達できず**（下記） |
| パロアルトネットワークス株式会社 | 57×57 | ✅ **192×192**（公式 favicon セット） |
| Ubie株式会社 | 70×70 | ❌ **調達できず**（下記） |

差し替えは [scripts/upload-logos-20260828c.mjs](../scripts/upload-logos-20260828c.mjs)。
**DB は触っていない**（3社とも `logo_url` は既に `.../logo.png` を指しており、
同じキーへ上書きした。オブジェクトの応答は `cache-control: no-cache` なので即反映）。

#### ⚠️★機械判定を通ったのに目視で落とした3件

**「公式サイトから 512×512 が取れた」を採用の根拠にしないこと。**
クアルコムに React のロゴを入れかけた同日の1件と同じ形で、**寸法・形式・発行元は全部正しかった。**

| 会社 | 何が取れたか | なぜ採らないか |
|---|---|---|
| **Ubie** | iTunes 公開APIで発行元 `Ubie, Inc.` の **512×512** | 中身が**製品アプリ「ユビー 医療AIパートナー」のアイコン**（キャラクター＋文字）。企業ロゴではない。公式サイトの apple-touch-icon は **70×70＝現行と同一**、Wikimedia は横長ワードマークのみ |
| **アリスタ** | iTunes で発行元 `Arista Networks, INC` の 512×512 | 中身が **CloudVision Installer**（製品）のアイコン。公式サイトは **406** でスクレイプ不可、favicon は 32×32、Wikimedia は 1115×175 の横長 |
| **ブラックライン** | 公式サイトのインライン SVG（97.85×100・ほぼ正方形） | `fill="#fff"`（暗い背景用）で、**白地に置くと見えない**。現行は金 (244,184,10) と黒の2色構成で公式SVGは単色1パス。**色と配置を推測することになる** |

⚠️ **横長のワードマークは採らない。** Wikimedia にあるのは 1115×175 / 421×124 / 512×202 で、
   入れると「白い正方形アイコンと色付きの横長バナーが混在する」という
   CLAUDE.md の**不揃い問題そのもの**になる。

⚠️ **16px 以下は0件**なので `CompanyLogo` の「失敗扱い」（16px 以下は頭文字に倒す）
   には入らない。**表示はされる。ぼやけるだけ。**

⚠️ **コードの問題ではない。** まともなロゴを調達して入れ直すしかない。
   直すなら公式サイト等から取得して差し替える。

⚠️ **2026-08-28 に実ファイルを取得して測り直した。6社とも上の表と同じ寸法のまま**
   （32×32 / 32×32 / 35×34 / 48×48 / 57×57 / 70×70）。**この項目は生きている。**
   同日のロゴ差し替え13社にはこの6社は入っていない（あちらは OGP画像だった企業）。

### ✅ ② PKSHA だけ Clearbit URL を消せない（**2026-08-28 に前提が消えた**）

当時は `ow_companies.url`（website）が null で、`logo_url` を null にすると
`CompanyLogo` が favicon 用のドメインを取り出せず頭文字だけになる、という話だった。

**実測（2026-08-28）: `url` は埋まっており、`logo_url` は既に null。**
Clearbit の URL は**全社で0件**（CLAUDE.md）。**この項目は消化されている。**
残っているのは「ロゴそのものを調達するか」だけで、①や「OGP画像のままの9社」と同じ話。

⚠️ `logo_url` が null の11社は現状のまま（頭文字＋グラデーション）。
   うち website があるのは6社なので、そちらは favicon が出る。

---

## RLS が `USING(true)` かつ anon に SELECT がある テーブル ── **個人データは片付いた**（2026-08-16 / **2026-08-27 に取り直し**）

25件を洗い出し、**個人データ・企業の内部情報・運営設定を12件すべて塞いだ**（うち1件は DROP）。
**残り13件は「公開が正しい」ものだけ。** ★塞ぎに行かないこと。理由を下の表に書いた。

### ⚠️ 2026-08-27 に数え直した ── **14表。個人が書いた文字列が入るものは0**

**⚠️ 以前この節に書いていた数え方は誤っていた。** `polroles` を見ておらず、
`service_role` にだけ配った `USING(true)` を「anon が読める」と数えていた
（**19表と報告したが、正しくは14表**）。数え方は CLAUDE.md
「RLS で弾かれても 403 ではない」に置いた。**そちらのクエリを使うこと。**

| 区分 | 件数 | 表 |
|---|---|---|
| **公開が正しいマスタ** | **12** | `ow_roles` / `ow_role_aliases` / `ow_tool_masters` / `ow_articles` / `ow_job_roles` / `ow_job_requirements` / `ow_job_matching_tags` / `ow_company_culture_tags` / `ow_company_office_photos` / `ow_company_segments` / `ow_company_tools` ＋ **`ow_languages` / `ow_skills`**（2026-08-27 に追加。意図どおり） |
| **個人が書いた文字列** | **0** | ~~`ow_story_sections`~~ … **2026-08-27 に塞いだ**（`20260827230000`。own + admin、anon の GRANT も剥奪） |

⚠️ **`ow_company_hidden_experiences` は対象ではなかった。** anon に効く SELECT ポリシーが
   元から無く、漏れていない（`USING(true)` は `TO service_role` のもの）。
   整理として anon の GRANT だけ剥がした（同 migration）。**ポリシーは触っていない。**

⚠️ **数え直すときは必ず `polroles` まで見ること。** 見ないと今回と同じ誤検出をする。

⚠️ **形は4通りに分かれた。一律に当てないこと。**

| 形 | 使ったテーブル |
|---|---|
| own + admin | 実績 / 受賞 / メディア掲載 / 発信コンテンツ / 学歴 / 経歴ストーリー（★親の職歴経由） |
| **authenticated**（ログイン済みなら読める） | 投稿 / いいね / **コメント** ← 他人が読む前提。集計列がある |
| **その企業のメンバー** | 求人の担当者（`auth_is_company_member`） |
| **admin のみ** | 運営設定 |

⚠️ **集計列（`likes(count)` / `comments(count)`）があるテーブルを own にしない。**
   読める範囲を狭めると **HTTP 200 のまま数字だけが小さくなる**。

### 塞いだもの（履歴。触らない）

| テーブル | 行数 | 形 | migration |
|---|---|---|---|
| `ow_user_achievements` / `ow_user_awards` / `ow_user_media_appearances` / `ow_user_content_links` | 0 | own+admin | 20260815130000 |
| `ow_posts`(170) / `ow_post_likes`(1) | — | authenticated | 20260815140000 |
| `ow_experience_roles`(6) | — | own（親の職歴経由）+admin | 20260815140000 |
| `ow_settings` | 0 | **admin のみ** | 20260816090000 + 091500 |
| `ow_job_assignees` | 0 | **企業メンバー** | 20260816100000 |
| `ow_experience_stories` | 0 | own（親の職歴経由）+admin | 20260816130000 |
| `ow_post_comments` | 0 | **authenticated** | 20260816150000 |
| ~~`ow_user_socials`~~ | 0 | **DROP** | 20260816140000 |

### 残り13件 ── すべて「公開が正しい」。★根拠つき

| テーブル | 行数 | 公開でよい理由 |
|---|---|---|
| `ow_articles` | 16 | 公開記事。未ログインの `/articles` で全文を出している |
| `ow_roles` / `ow_role_aliases` | 154 / 260 | 職種マスター。未ログインの求人検索・職種フィルタが引く |
| `ow_schools` | 37 | 学校マスター。学歴の入力候補（`/profile/edit`）と `/schools` で使う |
| `ow_tool_masters` | 78 | ツールマスター。企業ページの「使っているツール」の名前・ロゴ |
| `ow_job_roles` | 20 | 求人に紐づく職種。**求人自体が公開**なので、その属性も公開でよい |
| `ow_company_tools` | 9 | 企業が公開しているツール。企業ページに出す |
| `ow_company_culture_tags` / `ow_company_office_photos` / `ow_company_segments` | 各0 | **企業ページに出す公開情報**（オフィス写真・カルチャータグ）。個人データではない |
| `ow_job_matching_tags` / `ow_job_requirements` | 各0 | 公開求人の付帯情報 |
| `ow_story_sections` | 0 | 記事のセクション見出し。記事が公開なので同じ |

⚠️ **「0件だから未対応」ではない。** 上の13件は**値が入っても公開してよい**もの。
   個人データ・企業の内部情報・運営設定は**すべて処理済み**。

⚠️ ただし `ow_company_office_photos` は**企業側が書ける**表なので、
   書き込みポリシー（誰がアップロードできるか）は別途の関心事。読みは公開でよい。

## `ow_users` を副問い合わせしているポリシーを SECURITY DEFINER 関数にする（2026-08-19 記録・未着手）

**2026-08-19 に `ow_users` の anon 露出を塞いだ（`20260819100000`）が、
採ったのは「列単位 GRANT に置き替える」案。anon の SELECT 権限そのものは残っている。**

### なぜ全部剥がせなかったか

ポリシー式は**実行ユーザーの権限で評価される**（PostgreSQL: CREATE POLICY / Notes）。
`REVOKE SELECT ON ow_users FROM anon` を単純にやると、
**ポリシー式の中で `ow_users` を副問い合わせしている15表**が、anon から丸ごと403になる。

| | 実測（2026-08-19） | **実測（2026-08-28）** |
|---|---|---|
| 対象の表 | **15**（`ow_bookmarks` `ow_career_profiles` `ow_casual_meetings` `ow_company_join_requests` `ow_company_posts` `ow_conversation_messages` `ow_conversation_participants` `ow_conversations` `ow_job_applications` **`ow_jobs`** `ow_matches` `ow_mentor_reservations` `ow_notifications` `ow_user_follows` `ow_user_recommendations`） | **13** |
| ポリシー本数 | **19** | **15** |
| ポリシーが参照する `ow_users` の列 | `id`(37本) / `auth_id`(35本) / `visibility`(2本) の3つだけ | — |

⚠️ **2026-08-28 に測り直したら減っていた**（表 15→13 / ポリシー 19→15）。
   ⚠️★**数え方を間違えないこと。** 「`ow_users` を参照するポリシー」を条件なしで
      数えると **89本**出るが、それは別の指標（anon が読めない表も含む）。
      **この項目が言っているのは「anon が読める表の SELECT 系」だけ。**

```sql
select count(distinct c.relname) as 表, count(*) as ポリシー
  from pg_policy p join pg_class c on c.oid = p.polrelid
 where c.relnamespace='public'::regnamespace
   and p.polcmd in ('r','*')
   and (p.polroles = '{0}'::oid[] or 'anon'::regrole = any(p.polroles))
   and has_table_privilege('anon', c.oid, 'SELECT')
   and pg_get_expr(p.polqual, p.polrelid) ~ '\mow_users\M';
```

⚠️ とりわけ **`ow_jobs`**（`ow_jobs_company_admin_manage` が FOR ALL / PUBLIC）。
   `/companies` の「募集中 N件」「募集あり」・sitemap・LP が anon キーでここを読む。
   **403 は `?? []` で受けているので、画面は落ちず数字だけが静かに消える。**

### ⚠️★2026-08-28 に8本を置き換えた（**完了ではない**）

`20260828080000_policies_use_auth_ow_user_id.sql` で、
**式が `ow_users` の副問い合わせ「だけ」の8本**を `auth_ow_user_id()` に置き換えた。

| | |
|---|---|
| 置き換えた | `ow_bookmarks_own` / `ow_casual_meetings_seeker_read` / `users can read own applications` / `user_follows_own_manage` / `notifications_select_own` / `user can manage own reservations` / `Users can view their own join requests` / `recommendations_owner_read` |
| **残した** | **7本**（下記） |

### ⚠️★残した7本と、その理由（「ついでに」揃えないこと）

**【B】`ow_company_admins` 経由の4本**
`ow_casual_meetings_company_read` / `ow_matches_company_read` /
`company_admin_all`（`ow_company_posts`）/ `company_admins_read_applications`

⚠️★**`auth_is_company_admin()` に置き換えてはいけない。**
   あの関数は **`permission = 'admin'` を追加で要求する**が、この4本は
   **`is_active` だけ**を見ている。実測（2026-08-28）: `ow_company_admins` は
   admin 10（有効）/ admin 1（無効）/ **member 1（有効）**。
   置き換えると**その member が面談・応募・マッチを見られなくなる。**
   **権限の意味が変わる置換は「同じことの言い換え」ではない。**

**【C】参加者判定と admin ロールの複合3本**
`ow_conversation_messages_select` / `ow_conversation_participants_select` /
`ow_conversations_select`

⚠️ `ow_conversation_participants` や `ow_company_admins` との結合が混ざっており、
   `ow_users` の部分だけ置き換えても**表の権限要求は消えない**。
   **中途半端に置き換えると「直った」と誤認する。**

### ✅★残り7本を片付けた（2026-08-28 / `20260828140000`）── **対象 7 → 0**

**7本を2通りに分けた。理由が違うので同じ手を当てない。**

| | 表 | どうしたか |
|---|---|---|
| **①** | `ow_company_posts` | **ポリシーの式を書き換えた**。同じ表に `public_read_published_posts`（`is_published = true` / 全ロール）があり、**anon が公開投稿を読むのは意図どおり**（実測でも anon に1件返る）。権限は剥がせない |
| **②** | `ow_casual_meetings` `ow_conversations` `ow_conversation_messages` `ow_conversation_participants` `ow_job_applications` `ow_matches` | **anon から SELECT を剥がした**。実測で anon には全部 200/0件、**src に anon 経由の参照が1つも無い**（admin か認証済みセッションのみ） |
| **③** | `ow_message_reads` | **巻き添えの1件**。`ow_message_reads_select` が `ow_conversation_participants` を副問い合わせしている。**src からの参照0件**なので一緒に剥がした |

⚠️★**`auth_is_company_admin()` に寄せなかった。** あれは `permission='admin'` を追加で
   要求するが、`company_admin_all` は **`is_active` だけ**を見ている。
   → **`auth_is_active_company_admin(company_id)` を新設**（`is_active` のみ・
     `SECURITY DEFINER` / `row_security = off` / `search_path` 固定）。

   **寄せていたら実害が出ていた**（適用前に SQL で確認）:

   | | 結果 |
   |---|---|
   | 新しい関数の条件・**自社** | **true** |
   | 新しい関数の条件・他社 | false |
   | **`auth_is_company_admin()` の条件（permission='admin'）・自社** | **false ← 締め出されていた** |

   該当は **柴さんご本人**（株式会社Third Box の `member`）。

### ⚠️ 検証（2026-08-28 / 実際に PostgREST を叩いた）

| 表 | anon | 認証(一般) | 運営 |
|---|---|---|---|
| 剥がした7表 | **401（42501）** | **200** | **200**（`ow_conversations` と `ow_conversation_participants` は **1件**） |
| `ow_company_posts` | **200 / 1件** | 200 / 1件 | 200 / 1件 |

⚠️★**運営に1件返っているのが陽性対照。** 全部0件だと「遮断された」のか「元から空」なのか
   区別できない（CLAUDE.md「RLS で弾かれても 403 ではない。200＋0件が返る」）。
   **行数ではなくステータスで権限を判定した**（200 = 権限あり / 401 = 権限なし）。

⚠️ 画面も確認: 未ログインの `/` `/companies` `/companies/salesforce` `/jobs` `/jobs/[id]`
   `/sitemap.xml` が 200、ログイン済みの `/mypage` `/mypage/conversations`
   `/mypage/applications` `/people` も 200。
   （`/companies/third-box` の 404 は `is_published=false` / `is_test=true` のためで**無関係**）

### ✅★結論: `REVOKE SELECT ON ow_users FROM anon` は **やらない**（2026-08-28 に判断）

**この項目のもともとの目標（「そのうえで anon から SELECT を完全に剥がす」）は誤りだった。**
剥がすと**「公開プロフィール」機能が壊れる。**

#### なぜ剥がせないか

`ow_users` の RLS には **`ow_users_public_read`（`visibility = 'public'`・全ロール）** があり、
**anon が公開プロフィールを読むのは設計どおり**。しかも `/u/[id]` はそれを
**RLS を 404 のゲートとして使っている**（`page.tsx` の注記:
「admin に変えると非公開プロフィールが誰でも開ける」）。

| 実測（2026-08-28） | |
|---|---|
| `visibility` の分布 | `login_only` **37** / `private` **1** / **`public` 0** |
| `public` は選べるか | **選べる**（`ProfileTab` の型・`admin/candidates` の `VALID_VISIBILITY` とも3値） |
| `/u/[id]` が anon で読む13列の権限 | **13列すべて anon に SELECT がある**（＝いま機能する状態） |

→ 剥がすと、**誰かが「公開」を選んだ日に、未ログインからは 401 になり 404 に化ける。**
   いま 0名なので**気づけない**。CLAUDE.md「1人でも public にした瞬間に露見する類の壊れ方」。

#### したがって、いまの状態が**最終形**

2026-08-19 に採った「**列単位 GRANT に置き替える**」（anon 23/33列・
`email` / `birth_date` などを落とす）は**暫定ではなく、これが正解**だった。
そこに 2026-08-28 の「anon が読める表のポリシーから `ow_users` の副問い合わせを消す」
が加わって、**この項目は完了**。

⚠️ **「まだ剥がせていない」と読んで剥がしにいかないこと。** 機能が壊れる。
⚠️ 剥がすなら、それは**「公開プロフィールをやめる」という製品判断**とセットになる。

⚠️ 別に見つけた小さな懸念（**この項目とは別**）:
   `ProfileEditor.tsx:256` が `owUser?.visibility ?? "public"` で、
   **visibility が NULL のとき編集画面の既定が「公開」になる。**
   実データは全38件が非 NULL なので現時点で実害は無いが、
   **既定を「公開」に倒すのは向きが逆**。列は NOT NULL ではない。

---

### （元の記録）やること

19本のポリシーの `IN (SELECT ow_users.id FROM ow_users WHERE auth_id = auth.uid())` を、
既存の SECURITY DEFINER 関数 **`public.auth_ow_user_id()`**（anon 実行可）に置き換える。
関数は所有者権限で走るので、呼ぶ側に `ow_users` の SELECT 権限が要らなくなる。
そのうえで anon から SELECT を完全に剥がす。

⚠️ **「anon は revoke / authenticated は grant / RLS で絞る」（CLAUDE.md）に戻すのが本来の形。**
   いまは anon にも23列を配っているので、`ow_users_public_read`（`visibility='public'`）が
   anon にも効く状態が残っている。

⚠️ **やるときは、19本を1本ずつ。** ポリシーの意味が変わっていないことを
   anon / 非admin / 本人 / admin の4者で毎回測る（CLAUDE.md「最低3者で実測する」）。
   migration には `SET LOCAL ROLE anon` で15表を全部引く事後チェックを入れる
   （`20260819100000` と同じ形。**ロール切り替えはトップレベルに書く**）。

## `ow_profiles` の孤児20件 ── **原因を特定した**（2026-08-28）

`ow_profiles.user_id`（**auth 空間**）に対応する `ow_users` の行が無いもの。
実測（2026-08-28）: `ow_profiles` **52** / `ow_users` **38** / 孤児 **20**（2026-08-19 と同数）。

### ★正体は「消されたユーザーの残骸」ではなく、**シードデータ**

⚠️ **20件とも `auth.users` に今も存在する**（実測: 全件 1 件ヒット）。
   つまり「本番で auth ユーザーを消した痕跡」ではない。**消し方の心配は要らなかった。**

| | |
|---|---|
| メールアドレス | **20件すべて `@example.com`**（`tanaka.shota@` / `sato.misaki@` …） |
| 作成 | **2026-04-09 08:14:21〜23 の約2秒**（`ow_profiles` 側は 08:18:16〜17）＝一括投入 |
| `email_confirmed_at` | 全件あり |
| `last_sign_in_at` | **19件が NULL**。1件（`ikeda.hiroshi@`）だけ 2026-04-28 に1回 |
| 中身 | `onboarding_completed` 全件 false / 希望条件も `career_stance` も全件空 |

⚠️ CLAUDE.md の週次メールの節にある「**example.com 20件は必ずハードバウンスする**」と
   **同じ20件**。`getWeeklyRecipients()` が除外しているので実害は出ていない。

⚠️ `ikeda.hiroshi@` だけログイン履歴があるのに `ow_users` が無いのは、
   当時 `linkOwUser` の経路がまだ無かったため（`postAuth` は 2026-08-14）。

### ✅ **2026-08-28 に20件すべて削除した**（柴さんの承認 / `20260828110000`）

**実測（適用ログ）**: `auth.users` **60 → 40** / `ow_profiles` **52 → 32** /
`ow_users` **38 のまま**（巻き込みなし）/ **孤児 0**。

⚠️ 削除前に **`auth.users` を参照する FK を持つ29テーブル全部**で対象20件を数え、
   **`ow_profiles` の20行以外はすべて0件**であることを確認した。
   NO ACTION の6テーブル（応募・企業・請求・保存企業・保存求人・スカウト）も0件。

⚠️★**`ow_profiles.user_id` は `auth.users` への FK を持ち、削除規則は CASCADE。**
   ただし migration では**連鎖に任せず `ow_profiles` を先に明示的に消している**
   （何行消えるつもりかを書いておかないと、後から読んだ人が誤読する）。
   ⚠️ この FK は **`information_schema.constraint_column_usage` では見えない**
      （参照先が別スキーマのため）。**`pg_constraint` で見ること。**
      2026-08-28 に一度「FK が無い」と誤判定しかけた。

退避: `.dumps/20260828-1625-ow_profiles.sql` と `.dumps/20260828-auth-orphans.json`。
⚠️ 戻すときは auth ユーザーを作り直すので **id は変わる**。中身の記録として残しているだけ。

### （以下は判断の経緯。記録として残す）

**放置しても壊れない**（除外は効いている）。ただし
**`ow_profiles` 52 と `ow_users` 38 の差**が数え直しのたびに混乱を生んでおり、
実際に 2026-08-10（週次メール）と 2026-08-19（この項目）の**2回**調べ直している。

⚠️ **消すなら auth ユーザーごと。** `ow_profiles` だけ消すと `auth.users` に
   20件残り、同じ差が別の形で出る。
⚠️ **`ow_users` を参照する FK 45列のうち29列が `ON DELETE CASCADE`。**
   ただしこの20件は **`ow_users` の行を持たない**ので、巻き込む先が無い
   （それでも消す前に `ow_profiles` と `auth.users` のダンプは取ること）。

## 自由入力の企業名を `company_id` に寄せる導線（2026-08-19 記録・未着手）

**回答者には何も求めない。運営側の作業として寄せる。**

| | 実測（2026-08-19 / 実ユーザー18件） |
|---|---|
| `company_id` あり（マスタ紐づけ） | 13（72.2%） |
| `company_text` のみ（自由入力） | **5（27.8%）** |
| 自由入力の異なり社名 | 5（重複なし） |

入社の決め手・退職理由の集計は `company_id` を辿るので、**自由入力の27.8%は集計に乗らない**。
表記ゆれを名寄せして集計に入れると**別会社を1社として数える事故**が起きるため、
集計側では落とし、**運営が `ow_companies.normalized_name` / `canonical_company_id`
（既にある運営用の列）で寄せる**。

⚠️ **入力UIで「マスタから選んでください」と強制しないこと。** 入口が重くなる。
   オンボーディングで自由入力を許した判断と揃える。

### ⚠️★2026-08-28 実測 ── **大幅に改善しており、残りは1件**

| | 2026-08-19 | **2026-08-28** |
|---|---|---|
| 実ユーザーの経歴 | 18 | **13** |
| `company_id` あり | 13（72.2%） | **12（92.3%）** |
| **自由入力のみ** | **5（27.8%）** | **1（7.7%）** |

残る1件は **「みずほ証券株式会社」**。

⚠️★**`ow_companies` にこの会社が存在しない**（`name` / `brand_name` /
   `normalized_name` を「みずほ」「mizuho」で検索して0件）。
   つまり **`canonical_company_id` で寄せる先が無い。**

→ **この項目は「名寄せ」ではなく「企業を新規作成するか」の判断になった。**

### ✅ **2026-08-28 に作った**（柴さんの指示 / `20260828120000`）

**実ユーザーの自由入力は 0 件になった。** 木村雅樹の経歴（2017-04〜2021-10）が
`company_id` で紐づき、企業ページへのリンクになった。

| 列 | 値 | 根拠 |
|---|---|---|
| `name` | みずほ証券株式会社 | — |
| `name_en` | Mizuho Securities Co., Ltd. | **EV 証明書の `O=`** |
| `url` | https://www.mizuho-sc.com/ | 同上（`CN=www.mizuho-sc.com`） |
| `industry_id` | **金融・保険** | `requires_business_domain = false` なので事業領域は不要 |
| `is_published` | **true** | 経歴のリンク先が要る。false だと**404 の行き止まり** |
| `listing_status` | **draft** | ディレクトリには載せない |

⚠️★**公式サイトは 403 で自動取得できない**（curl も WebFetch も）。代わりに
   **TLS の EV 証明書**で法人を検証した（EV は CA が法人実在性を審査する）:
   `O=Mizuho Securities Co., Ltd. / L=Chiyoda-ku / ST=Tokyo / serialNumber=0100-01-008687`
   （会社法人等番号）/ issuer `Cybertrust Japan SureServer EV CA G3`。

⚠️ **`description` / `founded_year` / `employee_count` / `headquarters_address` は NULL のまま。**
   証明書で分かるのは「千代田区」までで住所ではないし、設立年も従業員数も確認できていない。
   **それらしい値で埋めないこと。**

⚠️ 前例に揃えてある（海光電業株式会社・スマートキャンプ株式会社が同じ組み合わせ）。
   フィードの `company_joined` は `listing_status → 'listed'` でしか作られないので**投稿は出ない**。

**検証**: `/companies/mizuho-securities` は 200・`noindex` 付き／`/companies` の一覧に
「みずほ」**0件**／`/u/{木村雅樹}` の職歴から企業ページへのリンクあり。

### ⚠️★これを作ったことで、既存の不具合が2つ露見した（同時に修正）

**どちらも「みずほ証券が原因」ではない。従業員数が空の企業で前から起きていた。**

| 場所 | 何が起きていたか |
|---|---|
| **企業詳細の「従業員数」** | `mapCompany` が **`(row.employee_count as number) ?? 0`** で NULL を 0 に化けさせ、**未入力の企業に「従業員数 0名」**と出していた。空値の行は描画側が落とすので、**null にすれば行ごと消える**。実測: 空の企業は **5社・うち公開2社** |
| **`/jobs` の「社員数順」** | 同じ `?? 0` を使って `bE - aE` を計算していたが、**列は text**（「約200名」）なので**文字列の引き算＝NaN**。比較関数が NaN を返すので**並び順は事実上変わっていなかった** |

⚠️★**原因は型が嘘をついていたこと。** `Company.employee_count` は mock 由来で `number` だが、
   **DB の `ow_companies.employee_count` は text**。`as number` のキャストが
   その食い違いを隠していたので、**tsc も lint も何も言わなかった。**
   → 型を `string | number | null` に直したところ、**同じ形の箇所が芋づるで4件出た**
   （mock の絞り込み・並び替え・詳細文言・`/jobs` の並び替え）。

⚠️ 数が読めない企業は **-1 で末尾**に置く。**0 にしないこと**（「社員0名」と同じ扱いになる）。

**検証**（dev の実HTML）: みずほ証券は**従業員数の行ごと出ない**／
Salesforce「3,500名以上」・Ubie「220名（2026年4月時点）」は**従来どおり**。

## ~~CLAUDE.md と実測の差分~~（**2026-08-28 に反映済み**）

2026-08-19 に「本文を直すときにまとめて当てる」として溜めていたぶんを、
**2026-08-28 に実測し直して CLAUDE.md へ反映した。**

| 直した記述 | 旧 | 新（2026-08-28 実測） |
|---|---|---|
| 職種のトップレベル | 17件 | **18件** |
| `ow_experiences` | 14件 / 実人数5人 | **24件 / 実ユーザー13件・実人数4人** |
| 公開求人と子職種 | 18件 | **5件**（出典なし13件を取り下げたぶん） |
| 経歴のうち親職種のまま | 2件 | **10件** |
| `ow_experience_roles` | **「未配線」** | ★**誤り。配線済み**（下記） |

### ⚠️★いちばん大きな誤りは「`ow_experience_roles` は未配線」

「`types.ts` にしか現れずアプリから一度も読み書きされていない」と書いてあったが、
**`/api/jobseeker/experiences` が読みも書きもしていた**（GET で `is_primary` 降順、
POST/PUT で INSERT）。6件 → 10件に増えていたのは**実際に書かれていたから**。

⚠️ **「行が増えている＝誰かが書いている」に気づいたのがきっかけ。**
   件数を更新しようとして数え直さなければ、誤った記述のままだった。
   **数字を更新するときは、その数字が動いた理由も見ること。**

---

## ~~（旧）CLAUDE.md と実測の差分~~（記録として残す）

| # | CLAUDE.md の記述 | 実測（2026-08-19） |
|---|---|---|
| ① | 職種のトップレベルは「**17件**」（2026-08-10 実測） | **18件**（`parent_id is null` かつ `merged_into_id is null` かつ `is_active`） |
| ② | 「`ow_experiences` 14件 / 実人数5人」（2026-08-10） | **19件**（実ユーザー18件） |
| ③ | — | `careerReasons.ts` のコメント「DB 側でも GRANT を付けていないので admin 以外読めない」は **`ow_experience_gaps` については誤り**。同テーブルは `authenticated` に SELECT/INSERT/UPDATE/DELETE があり RLS で本人に絞っている（**2026-08-19 に当該コメントは修正済み**）。正しいのは `ow_experiences` の3列のほう |
| ④ | 「UI / API / DB の CHECK を3つ揃える」 | ~~`ow_experiences.employment_type` に CHECK が無い~~ → **2026-08-26 に解消**（上の節）。CLAUDE.md 側にも追記済み |

## ~~生年が2箇所にあり、年が食い違っている実ユーザーが1人~~（**2026-08-28 に解決**）

生藤 弘樹（`0c99e403-…`・実ユーザー）の生年が
`ow_users.birth_date` = **1996**-11-05 / `ow_career_profiles.birth_year` = **1991** で
5年ずれていた。**2026-08-28 に柴さんが「1996 です」と確定**したので、
`20260828130000` で `birth_year` を 1996 に寄せた（食い違い 0 件）。

⚠️ 画面に出るのは `ow_users.birth_date` の側だけなので、**表示は元から正しかった。**
   直したのは「食い違いが残っていること」自体で、表示バグの修正ではない。

⚠️ **NULL にはしなかった。** 消すと「本人が答えていない」のか「運営が消した」のかが
   分からなくなるため。⚠️ **ただし正は `ow_users.birth_date`。**
   `ow_career_profiles.birth_year` を新しく読み書きしないこと。

⚠️ `ow_career_profiles` は **全部で1行**（2026-08-28 実測）。その1行がこれだった。

## Storage はバックアップされていない（2026-08-20 記録・今回は実装しない）

**Supabase の日次バックアップに Storage は含まれない。** DB を巻き戻しても、
`storage.objects` の行は戻るが**実ファイルは戻らない**（参照だけが生き残る）。

### いま入っているもの（2026-08-20 実測）

| バケット | 公開 | ファイル数 | 合計 | 最終追加 |
|---|---|---|---|---|
| `ow-uploads` | 公開 | **103** | 16 MB | 2026-06-19 |
| `candidate-documents` | 非公開 | 2 | 1,947 kB | 2026-03-20 |
| `documents` | 非公開 | 0 | 0 | — |

`ow-uploads` の内訳: `companies/` **88件**（5,003 kB・企業ロゴとオフィス写真）/
`mentors/` 12件（6,719 kB）/ `users/` 3件（4,980 kB・アバターとカバー）。

⚠️ **企業ロゴ88件は、消えると画面から企業の見た目が失われる。**
   `components/common/CompanyLogo.tsx` は Clearbit の死んだURLから
   Google favicon にフォールバックするが、**Storage に上げ直した87社ぶんは戻らない**。

### 対処案（どちらも未実装）

| 案 | 中身 | 向き不向き |
|---|---|---|
| **A. 定期的にローカル/別ストレージへ同期** | `supabase storage cp -r ss:///ow-uploads ./storage-backup` を月次で回す。16 MB なので所要は数十秒 | **いまの規模ならこれで十分。** 手動でも回せる |
| B. アップロード時に二重書き込み | 保存 API から S3 等へも書く | 実装が増え、失敗時の扱いを決める必要がある。**規模に対して過剰** |

⚠️ **今回は実装しない。** まず A を1回手で回して、実際に戻せるかを確かめるのが先。

## Supabase 呼び出しの error 握りつぶし（2026-08-20 棚卸し・段階的に直す）

`?? []` が RPC の 404 を「0件」に化けさせた。**anon の 403 が `?? []` で素通りするのと
同じ形で、2度目**（1度目は 2026-08-19）。

### 規模（実測 2026-08-20）

| | 件数 |
|---|---|
| `.rpc(...)` の呼び出し | **24**（うち **error を受けていない 19**） |
| `.from(...)` の分割代入 | **533**（うち **error を受けていない 264**） |
| `data ?? []` / `data ?? null` | **131** |

⚠️ **264件を一斉に直すのは割に合わない。** 大半は admin クライアントの読みで、
失敗しても実害の無いものが多い。**危ない形だけ順に潰す。**

### 内訳（RPC 19件）

| 種類 | 件数 | 危険度 |
|---|---|---|
| `auth_is_admin` | **16** | 低。失敗すると「管理者ではない」に倒れる（**fail-closed**） |
| `can_send_scout`（`/biz/candidates`） | 1 | 中。失敗すると**その候補者が黙って一覧から消える** |
| `get_blocked_companies`（`/api/jobseeker/scout-settings`） | 1 | **高**。404 が「ブロック0件」に化けていた |
| `ow_scout_blocks` の直読み（同上） | 1 | 中 |

### 段階（案）

| 段 | 対象 | 状態 |
|---|---|---|
| **1** | 危険度 中・高の3件に `console.error` を足す | ✅ **2026-08-20 実施** |
| **2** | **anon キー経路**と**列単位 GRANT を剥がした表**を触るクエリ | ✅ **2026-08-20 実施**（6箇所。下） |
| 3 | `auth_is_admin` の16件 | **やらない**と決めた。fail-closed で実害が無く、直すより `lib/auth/isAdmin.ts` に集約するほうが筋。集約は別タスク |
| 4 | 残りの `.from()` 264件 | **やらない**と決めた。「新しく書くときは error を受ける」を規約にして、触ったついでに直す |

### 段階2で直した6箇所（2026-08-20）

| 場所 | クライアント | なぜ危ないか |
|---|---|---|
| `api/companies/batch/route.ts` の Promise.all 3本 | **anon**（`createPublicClient`） | `ow_users` を埋め込んでおり、許可外の列が1つ入ると403が丸ごと返る |
| `lib/seo/featuredCompanies.ts` の `ow_jobs` / `ow_articles` | **anon** | sitemap・LP の母集団が黙って0件になる |
| `lib/supabase/queries.ts` の `getJobPositionMembers` | session（**未ログインでは anon**） | `ow_users` の埋め込み。403 が `!expRows` で空になる |
| 同 `getCompanyPhotos` / `getCompanyRecruiters` の `ow_users` | admin | 403 にはならないが、列単位 GRANT の表なので揃えた |

⚠️ `companies/(list)/page.tsx` の在籍メンバー取得は**元から error を見ていた**
（2026-08-05 に同じ形で踏んでいるため）。

⚠️ 規約は CLAUDE.md「Supabase の呼び出しで error を捨てない」に書いた。

✅ **`get_blocked_companies` の本体も直っている**（2026-08-28 に確認）。
   この行は「まだ直していない」と書いていたが**古かった**。実測:

   | | |
   |---|---|
   | 関数の定義 | 引数は `p_auth_user_id`。`ow_experiences` は `ow_users` を join して `auth_id` で引く。`ow_scout_blocks.candidate_id` は auth 空間なので直接比較 |
   | 呼び出し側 | `admin.rpc("get_blocked_companies", { p_auth_user_id: user.id })` で**引数名が一致** |
   | 実行 | 実ユーザー3人で `error` なし・0/1/1件 |
   | 見る場所 | `/mypage/settings` の `PrivacySettings`（2026-08-27 に置き場ができた） |

   ⚠️ **この項目でやることは残っていない。** 段階3・4は「やらない」と決めてある。

## ★user_id の空間取り違え ── 関数を全件洗った結果（2026-08-20 実測・**全件修正済み**）

⚠️ **見出しは「1件は未修正」だったが、実測すると未修正は0件**（2026-08-28 に確認）。
   `get_blocked_companies` の引数は `p_auth_user_id` で、`ow_users` を join している。
   **本文中では既に修正済みと書かれており、見出しだけが古かった。**
   ⚠️ **この節は「見つけ方」として残す価値がある**ので消していない
      （下のクエリで同じ形をまた探せる）。

`can_send_scout` が1本すり抜けていたので、**public の関数を全件突き合わせた**。
手順は「関数の本体が参照している `user_id` / `candidate_id` 系の列を拾い、
**その列の FK が `auth.users` か `ow_users` か**を見て、
1つの引数が両空間の列と比べられていないかを数える」。

### 結果

| 関数 | 引数 | 判定 |
|---|---|---|
| `can_send_scout(p_company_id, p_candidate_id)` | auth | ✅ **2026-08-20 に修正済み**（`ow_users` を join） |
| **`get_blocked_companies(p_candidate_id)`** | auth（呼び出し側が `user.id` を渡す） | ❌ **同じ形の混在。未修正**（下） |
| ~~`has_worked_at_company(p_user_id, p_company_id)`~~ | ow_users で一貫 | ✅ **2026-08-20 に DROP**（`20260820180000`）。src からの呼び出しは0件、DB 内の呼び出し元2つ（`guard_salary_insert` / `guard_review_insert`）も**対象表が DROP 済みで trigger 0本**＝3本まとめて死んでいた |
| `get_public_career_steps(p_user_id)` | ow_users で一貫（`auth.uid()` は `ow_users.auth_id` 経由で橋渡し） | ✅ |
| `create_conversation(p_kind, p_candidate_user_id, …)` | ow_users で一貫 | ✅ **手本**。不一致なら `42501` を投げる明示アサートがある |
| `auth_is_company_admin` / `auth_is_company_member(target_company_id)` | 引数は company | ✅ |
| `approve_school_request` / `reject_school_request(p_approved_by)` | auth で受け、**関数内で `ow_users.auth_id` から解決**して書く | ✅ |
| `rebuild_ow_transitions()` / `update_company_member_counts()` | 引数なし | ✅ |

### ~~❌ `get_blocked_companies(p_candidate_id)` ── 2つ壊れている~~（2026-08-20 に両方修正）

**① 空間が混ざっている**（`can_send_scout` と同じ形）

```sql
from ow_experiences e ... where e.user_id = p_candidate_id   -- ow_users 空間
...
from ow_scout_blocks b ... where b.candidate_id = p_candidate_id  -- auth 空間
```

呼び出し側（`/api/jobseeker/scout-settings`）は `user.id`（**auth**）を渡すので、
**在籍企業のぶんは一度も返っていない**。手動ブロックだけが返る形。

**② 呼び出し側の引数名が違う → そもそも 404 になっている**

```ts
admin.rpc("get_blocked_companies", { candidate_id: user.id })  // ← 関数の引数は p_candidate_id
```

実測（service role で PostgREST を直接叩いた）:

| 渡した引数名 | 応答 |
|---|---|
| `candidate_id`（アプリと同じ） | **404 `PGRST202`**「no matches were found」 |
| `p_candidate_id`（正しい） | 200 `[]` |

⚠️ ルートは `blockedResult.data ?? []` で受けているため、
**404 が「ブロック企業0件」として静かに素通りしている。**
スカウト設定画面の「ブロック中の企業」は**常に空**。

**★修正済み（`20260820200000`）。2つ同時に直した。**

- 空間 … `ow_users` を join して `u.auth_id = p_auth_user_id` で引く
- 引数名 … 規約に合わせて **`p_auth_user_id`** に改名し、**呼び出し側も同じ名前に**した
- ★**「在籍企業」は現職だけ**にした。同日 `can_send_scout` を「現在在籍していない」に
  変えたので、**この一覧が同じ基準でないと「ブロック中と出ているのにスカウトが届く」**になる

**実測（2026-08-20）**

| | |
|---|---|
| 旧の引数名 `candidate_id` で呼ぶ | **404 `PGRST202`**（＝これまでずっとこれ） |
| 新の引数名 `p_auth_user_id` で呼ぶ | **200** |
| ブロックが1件以上出る実ユーザー | **6人**（内訳: `company_id` 一致 4 / 自由入力の名寄せ一致 2） |
| 手動ブロック | 0件（`ow_scout_blocks` が空） |
| API 応答（is_test の企業アカウント） | `blocks` に「株式会社セールスフォース・ジャパン / experience」が**1件返るようになった**（従来は `[]`） |

⚠️ **画面での確認はできていない。** この API の唯一の読み手 `SettingsTab.tsx` は
   2026-08-17 にタブごと外れており、**ブロック一覧を出す画面が現在どこにも無い**。
   置き場を決めること（`/mypage/settings` に公開範囲と一緒に置くのが自然）。

### この形が見つけにくい理由（次に読む人へ）

- **tsc も lint も通る。** 型は uuid どうしで一致する
- **エラーも出ない。** 条件が1つ静かに効かなくなるだけ
- **RPC の引数名違いは 404 だが、`?? []` で受けていると0件と区別がつかない**
- ⚠️ **実データで数えるまで見つからない。** CLAUDE.md に警告があっても防げていなかった
  （文章では防げない、が今回の結論）

### 見つけ方（このクエリを残す）

```sql
with spaces as (
  select c.relname tbl, a.attname col,
    case when d.confrelid='auth.users'::regclass then 'auth'
         when d.confrelid='public.ow_users'::regclass then 'ow_users' end sp
  from pg_attribute a
  join pg_class c on c.oid=a.attrelid and c.relnamespace='public'::regnamespace and c.relkind='r'
  join pg_constraint d on d.conrelid=a.attrelid and d.contype='f' and a.attnum=any(d.conkey)
  where a.attnum>0 and not a.attisdropped
    and d.confrelid in ('auth.users'::regclass,'public.ow_users'::regclass)
)
select p.proname, string_agg(distinct s.sp,' + ') as 空間,
       string_agg(distinct s.tbl||'.'||s.col||'('||s.sp||')', ', ') as 内訳
from pg_proc p join spaces s
  on pg_get_functiondef(p.oid) ~ ('\m'||s.tbl||'\M')
 and pg_get_functiondef(p.oid) ~ ('\m'||s.col||'\M')
where p.pronamespace='public'::regnamespace and p.prokind='f'
group by p.proname having count(distinct s.sp) > 1;
```

⚠️ これは**候補を挙げるだけ**。橋渡し（`ow_users.auth_id` で解決している）も引っかかるので、
   **1本ずつ本体を読んで判定する**こと。

## ★migration は復旧手段ではない（2026-08-20 確立・要判断）

**`supabase/migrations/` を最初から流し直しても、このDBは再現できない。**

| | 実測（2026-08-20 / 素の supabase/postgres:17 に112本を順に適用） |
|---|---|
| 通った | **54本** |
| 落ちた | **58本** |

落ちた58本の大半は**設計どおり**で、データ migration が
「対象が想定件数と違う → 中止」と自分で止まる（本番の行数を前提にした事前チェック）。
加えて**企業データは migration ではなく `supabase/seeds/` から入る**ため、
schema と data が1本の鎖になっていない。

⚠️ **適用済みの migration を書き換えない方針は維持する。** 直すなら「復旧手段を別に持つ」側。

### いま分かっていること

| | 実測（2026-08-20） |
|---|---|
| WAL アーカイブ | **稼働中**（`archive_mode=on` / `wal_level=logical` / `archive_command` は wal-g） |
| アーカイブ実績 | `archived_count` **6,441** / 最終 2026-08-19 16:13 UTC / 失敗3（最後は 2026-08-10） |
| `supabase/seeds/` の鮮度 | 最新でも **2026-05-28**（10社ぶん）。本番は **87社**。**復旧には使えない** |

⚠️ **WAL が流れていることは PITR が使えることを意味しない。** Supabase の日次バックアップも
   同じ仕組みを使う。**PITR アドオンが有効かどうかは DB からは判定できない。**

### ★バックアップの現状（2026-08-20 ダッシュボードで確認・確定）

| | |
|---|---|
| 日次バックアップ | **8日分**（Supabase Pro / PHYSICAL） |
| **PITR** | **無効**（有料アドオン。Point in time タブで確認） |
| Storage | **バックアップ対象外**（下の節） |

### ★判断：PITR は当面入れない（2026-08-20 決定）

**「検討していない」のではなく、検討したうえで見送った。**

理由: 遷移5件・実ユーザー14人・職歴19件という段階では、
**月額を払うより「作業前ダンプ」の習慣のほうが費用対効果が高い**。
1テーブル単位で戻せるという点でも、いまの事故の形（migration で1表を壊す）に合っている。

⚠️ **失って困るユーザーデータが増えたら再検討する。** 目安になる出来事:

- 実ユーザーが増え、**本人しか持っていない入力**（職歴・理由データ・希望条件）が積み上がったとき
- 応募・スカウト・メッセージなど、**やり取りの履歴**が動き始めたとき
- 「昨日の何時に壊した」を1日単位で戻すと**他の利用者の入力まで巻き戻す**規模になったとき

⚠️ 見送っている間の代替は **`./scripts/dump-tables.sh`**（CLAUDE.md に手順あり）。
   **DB を変える migration の前に必ず取る。**

### 復旧の選択肢（メモ）

**`Restore to new project`（BETA）** … バックアップから**新しいプロジェクトを作る**機能。

⚠️ **本番を巻き戻さずに、過去時点の中身を確認できる。**
「昨日この列はどうなっていたか」「消した行に何が入っていたか」を安全に調べたいときに使う。
本番の Restore（全体巻き戻し）を選ぶ前に、まずこちらで中身を確かめる。

⚠️ CLI のアクセストークンは macOS キーチェーンにあり、Management API を
   このリポジトリから叩く手段は無い（`~/.supabase/` にトークンファイルは無い）。
   **バックアップ設定はコードからは触れない。ダッシュボードで見る。**

## 候補者一覧に出ない実ユーザーの内訳（2026-08-20 調査・データは変更していない）

⚠️★**母数が変わっている。** 記載の「職歴がある実ユーザー**9人**」は
   2026-08-28 の実測で **4人**（`is_test` の付け直しで入れ替わった）。
   `ow_profiles` が無い2人は変わらず。
   ⚠️ **件数を根拠に判断する前に必ず測り直すこと。**

職歴がある実ユーザー**9人**のうち、`/biz/candidates` に出るのは**5人**。落ちている4人は2種類。

### ① `ow_profiles` の行が無い（2人）── バグではなく取り残し

**行が作られる経路は2つしかない。**

| 経路 | いつから |
|---|---|
| `auth.users` INSERT の trigger `handle_new_ow_user()` が `ow_profiles` を作る | **2026-08-04 から** |
| オンボーディングの `finish`（「登録して始める」も「後で設定する」も同じ） | 以前から |

該当2人は**どちらも 2026-08-04 より前に作られた行**（2026-06-03 / 2026-06-18）で、
trigger が無かった時期のもの。内訳は

- **1人は `auth_id` が NULL**（運営が先に作った行。本人がまだサインアップしていない）
- **1人は `auth_id` あり**（サインアップ済みだがオンボーディング未完了）

⚠️ **後者は、いまのままだと永久に候補者検索に出ない。**
   オンボーディングを最後まで通せば行ができる（「後で設定する」でも作られる）。

⚠️ **新規登録では起きない**（2026-08-04 の trigger が塞いでいる）。

### ② `scout_enabled` が null（2人）── 「拒否」ではなく「未選択」

列コメント（`ow_profiles.scout_enabled`）に明記されている:

> 2026-08-04 から新規は既定 true。null は 2026-08-04 以前に作られた行の「未選択」で、
> `can_send_scout` の `coalesce(...,false)` により受け取らない扱いのまま。
> **本人が選んでいないため遡って true にはしない。** 未選択者には /mypage のバナーで選択を促す。

⚠️ **データは変更しない。** 本人が選んでいない状態を運営が「受け取る」に倒すと、
   同意していないスカウトが届く。促し（`/mypage` のヘッダー下ボックスの「スカウト｜未選択」）
   が機能しているかを見るのが先。

## 希望勤務地（`desired_prefectures`）がマッチングに使われていない（2026-08-15 記録）

**対象**: `ow_profiles.desired_prefectures`（2026-08-15 のフェーズ2で追加）。
入力は `/profile/edit` の「希望勤務地・勤務スタイル」カード、
表示は `/biz/candidates` の候補者カード（「希望勤務地: 東京都・神奈川県」）。

**なぜ残っているか**: **入力させて表示までしているのに、絞り込みにも推薦にも使っていない。**
実測（2026-08-15）:
- `src/lib/matching/scoreJob.ts` の軸は **職種48 / 年収30 / フェーズ24 / 勤務形態18 の4つだけ**で、
  **勤務地の概念が無い**（`desired_prefectures` を1度も読んでいない）
- `/biz/candidates` は `desired_prefectures` を select して**表示しているだけ**。
  絞り込みの条件には入っていない

⚠️ この状態を放置すると「入力させたのに使われない」になる（CLAUDE.md の禁止事項）。
   列と入力欄が先にできているぶん、忘れると気づけない。

**やるなら**（3つセットで考える）:
1. `scoreJob` に**勤務地の軸を新設**する。⚠️ 重みは4軸の合計と `MIN_SCORE`（30）に影響するので、
   足すだけでなく既存の重みを見直すこと。理由文（`reasonParts`）も1行足す
2. ★**「フルリモートは全都道府県を対象にするか」をそこで決める。**
   決めずに実装すると、フルリモート希望者が勤務地の軸で必ず減点される
   （`desired_work_styles` に `full_remote` を持つ人の扱い）
3. `/biz/candidates` の絞り込みに `desired_prefectures` を足す（今は表示のみ）

⚠️ 求人側の勤務地は `ow_jobs.location`（自由文字列）で、都道府県の正規化がされていない。
   軸を作る前に、求人側の値が都道府県として突き合わせられる形かを先に確かめること。

### ⚠️★2026-08-28 実測 ── **いま作っても差がつかない**

前提条件（「求人側の値が突き合わせられる形か」）を測った結果、**両側とも材料が無い**。

| | 実測 |
|---|---|
| 公開求人 | **5件。`location` は全件「東京都」**（種類が1つしかない） |
| **`desired_prefectures` を入れている人** | **0人** |
| `full_remote` を希望している人 | 1人 |

→ **軸を足しても、全員が同じ条件で当たるだけ**（`/companies` の「開示充実順」が
   `reality_disclosure` 全社同値で並べていたのと同じ形）。

⚠️ **「入力させたのに使われない」は依然として残っている問題**だが、
   **入力している人が0人**なので、いま実害は出ていない。

**着手の条件**（どちらも満たしてから）:
1. `desired_prefectures` を入れている人が**複数いる**
2. 公開求人の `location` が**2種類以上**ある（＝正規化の要否がそこで決まる）

⚠️ 求人が「全件 東京都」なのは、公開5件がすべて Salesforce だから
   （CLAUDE.md「出典なし13件を取り下げた」経緯）。**求人が増えるまで動かない。**

---

## ~~★StoryAccordion が事故で孤児になっている~~（**2026-08-28 に解消を確認**）

⚠️ **孤児ではない。** `mypage/details/[section]/CareerDetails.tsx` が import して
   描いている（`e2208179` で復帰）。実測で import 元 **2件**（2026-08-28）。
   ⚠️ **この節は次に判断するときの材料として残す。** 片付いたと決めたら行ごと消してよい。

**経歴ストーリー（`ow_experience_stories`）の編集UIが、画面から到達できない。**

`git log -S` で追ったところ、参照が外れたのは
**`e80616ad`「職歴を公開プロフィールと同じ年表にする（2-6）」**（2026-08-16）。
`CareerHistoryEditor` の自前一覧（229行）を `MergedTimeline` に差し替えたとき、
その中で描いていた `<StoryAccordion experienceId={stint.id} />` **ごと消えた**。
コミットメッセージに StoryAccordion の話は無く、**機能を畳む判断はされていない。**

| | |
|---|---|
| ファイル | `src/components/profile/StoryAccordion.tsx`（**2,111行**） |
| 参照 | **0**（2026-08-20 実測） |
| DB | `ow_experience_stories` / `ow_story_sections` とも**0行**。RLS は `20260816130000` で own+admin に締めた |
| Storage | `ow_uploads_can_write` の③が `{auth.uid()}/experience-stories/…` を許可しており、**コメントに「アプリ側（StoryAccordion）がこのパスで書いている」と書いてある** |

⚠️ **今回の掃除では消していない。** 2,111行の機能まるごとの去就は、
   dead code の整理ではなく**製品の判断**だから。決めるのは次の2つ。

1. **戻す**なら、職歴カードのどこから開くかを決めて `MergedTimeline` 側に導線を足す
2. **畳む**なら、ファイルだけでなく **Storage ポリシー③のコメントと、
   `ow_experience_stories` / `ow_story_sections` の扱い**も一緒に決める
   （どちらも0行なので、DROP するかは別途）

## ~~CompanyCardCompact は本体が未使用・型だけ生きている~~（**2026-08-28 に削除済み**）

`GenreCarousel` を削除した結果このカードを描画する画面が無くなり、
`MemberPreview` 型のためだけにファイルが残っていた。

**手順1〜3は不要になった。** 同日の `a5f40cac`（常に空になるメンバー取得の除去）で
`MemberPreview` を使う3箇所（`CompanyCardList` の import・`page.tsx` のローカル定義・
`members` prop）がまとめて消え、**型の置き場を決める必要が無くなった**ため、
`CompanyCardCompact.tsx` をそのまま削除した（286行）。

⚠️ **当時の記録に誤りがあった。** 「`RecentlyViewedSection` が import している」と
   書いていたが、**実際には import していなかった**（2026-08-28 に grep で確認）。
   唯一の import 元は `CompanyCardList` だった。

あわせて片付けたもの:

| | |
|---|---|
| `globals.css` の `.ccc-compare-btn` 3ルール | **削除**。`ccc-` は CompanyCardCompact の中でも使われておらず、**削除前から死んでいた** |
| `globals.css` の `.genre-card` | **残す。** `CompanySearchResults` が自前の `<style>` で再定義したうえで使っている（絞り込み結果のビュー） |
| `stageCfg.ts` / `CompanyCardList.tsx` のコメント | 現在形で「別実装がある」「共有している」と書いていたので**訂正** |
| `companyBookmarks.ts` / `displayName.ts` のコメント | 過去の経緯の記録なので**残し、ファイルが無いことを注記** |

## ~~social_links に URL 形式の検証が無い~~（**2026-08-28 に解消**）

**「壊れた行を先に 0 件にしてから、素直に 400 を入れる」で片付いた。**
todo に挙げていた3案（A: 変更キーだけ検証 / B: 黙って落とす / C: UI に印）は**どれも要らなかった。**

| | |
|---|---|
| データ | `20260828100000_fix_broken_social_link.sql` で唯一の壊れた行（`github: "a"`）を**キーごと落とした** |
| API | `optionalUrlMap`（[lib/api/normalize.ts](../src/lib/api/normalize.ts)）を足し、`PUT /api/jobseeker/profile` が使う |
| UI | **既に足りていた** —— 入力欄は `type="url"`、placeholder は7サービスとも `https://…`（`SOCIAL_META`） |

⚠️★**順序を逆にしないこと。** `social_links` はこの PUT に**他の項目と一緒に載る**ので、
   壊れた値が1つでも残ったまま検証を入れると、**そのユーザーは名前の変更すら保存できなくなる。**

### ★DB の CHECK は張らないと決めた（根拠を残す）

CLAUDE.md の「UI / API / DB の CHECK を3つ揃える」は**値の集合**（選択肢・状態・区分）が対象で、
これは**形式**の制約。しかも JSONB の全キーを見る CHECK は**素では書けない**
（`jsonb_each_text` を使うと副問い合わせになり、CHECK 制約は副問い合わせを許さない）。
関数に逃がせば書けるが、**テーブル定義を読んでも分からない隠れた挙動**が増える
（`ow_company_members` の `guard_member_consent` で実際に踏んでいる形）。

→ **UI と API の2層で守る。** 濃度の制約（`ow_user_skills` の15件）と同じ扱い。

### ⚠️ https のみ。http は通さない

`content-links` と揃えた（**todo の指摘そのものが「同じ外部リンクなのに扱いが揃っていない」だった**）。
SNS 7サービスはいずれも https でしか配信していないので、http を通して得るものが無い。
**緩めると画面の文言（「https:// で始まる URL を…」）と実装がずれる。**

### 実測（2026-08-28 / dev・is_test アカウントで実際に PUT）

| 入力 | 結果 |
|---|---|
| `{"github":"a"}` | **400** `INVALID_INPUT` |
| `{"x":"javascript:alert(1)"}` | **400**（⚠️ `new URL()` は通すので**プロトコルまで見ている**） |
| `{"x":"mailto:a@b.co"}` | **400** |
| `{"x":"http://example.com/a"}` | **400** |
| `{"x":"https://x.com/testuser"}` | 200 |
| `{"x":"https://…","note":""}` | 200（空文字はキーごと落ちる。従来どおり） |
| `{}` | 200（`null` になる） |

⚠️ 検証で書き込んだ is_test アカウントは `social_links = null` に戻したことを SELECT で確認済み。
   壊れた行は**全ユーザーで0件**。

---

## 完成度の配点（2026-08-16 に表示を廃止）

**プロフィール完成度バーの表示をやめた**（`/mypage` の右カラム、`/profile/edit` の
右カラム、どちらも削除）。配点は議論して決めた配分なので、消す前にここへ書き写す。

| 項目 | 配点 | キー |
|---|---|---|
| 職歴 | **30** | `career` |
| 希望条件 | **15** | `prefs` |
| プロフィール画像 | **11** | `avatar` |
| 学歴 | **10** | `edu` |
| 名前 | **8** | `name` |
| 自己紹介 | **8** | `aboutMe` |
| 肩書き（1行） | **4** | `headline` |
| 所在地 | **4** | `location` |
| 生年月日 | **4** | `birth` |
| 実績・受賞 | **3** | `certs` |
| SNS・発信 | **3** | `social` |
| **合計** | **100** | |

⚠️ **自己紹介は元は 12 点だった。** 一覧やスカウトで先に読まれるのは名前の直下の1行
   なので、8 に下げて**肩書きへ 4 を回した**。合計100は変えていない。

### 公開スコア（81点満点）は**残っている**

`calcPublicScore()` は `/people` の並び順で現役。**消さないこと。**
100点から「希望条件」(15) と「生年月日」(4) を外した 81点満点。

⚠️ 外している理由は重要度ではなく、**公開されない情報だから**。希望条件はスカウト用の
   非公開情報で、公開プロフィールにもカードにも出ない。並び順に入れると
   「非公開情報を埋めた人が公開一覧で上に来る」ことになり、見ている側からは
   理由の分からない順序になる。生年月日も同じ（年代フィルタに使うだけ）。
   ⚠️ `prefs` / `birth` をここに足さないこと。型でも防いである（`PublicCompletionInput`）。

### 何を消したか

`src/components/profile/ProfileCompletionBar.tsx`（表示そのもの）と、
`completion.ts` の `calcCompletion` / `ScoreItem` / `buildItems`。
`CompletionInput` は**非公開の型として残る**（`PublicCompletionInput` の土台）。

⚠️ **配点そのものはコードに残っている**（`completion.ts` の `ITEM_WEIGHTS`）。
   `calcPublicScore` が使うため。消えたのは「100点満点の合計を出して画面に並べる」ほうだけ。
   上の表はその写しで、**画面を作り直すときに数字を作り直さないため**に置いてある。

⚠️ 表示を戻すときは `git log -- src/components/profile/ProfileCompletionBar.tsx` から辿れる。

---

## 学歴の日付まわりで、画面に「無い」と分かる形になっていない2件（2026-08-16 記録・2-5 で実測）

どちらも**移行前からある挙動**。2-5 で `/mypage` の学歴が公開部品（`MergedTimeline`）
に切り替わったときに実測で見つかった。**2-5 では直していない。**

### ① 入学年月が無い行は、公開プロフィールから黙って消える

`toTimelineEducationEntries`（[src/lib/utils/timeline.ts](src/lib/utils/timeline.ts)）は
`enrolled_at` が無い行を `filter` で落とす。年表に置けないため。

⚠️ **実データに1件ある**（2026-08-16 実測 / 全11件中1件・`is_test` ではない実利用者）。
   本人にも他人にも「載っていない」ことは伝わらない。

`/mypage` 側は 2-5 で拾うようにした（「入学年月が未入力のため、公開プロフィールには
表示されていません」と出して鉛筆・ゴミ箱を付ける）。**公開側は未対応**。
そもそも `enrolled_at` を必須にするか（入力UI・API・DB の3つを揃える）を先に決める。

### ~~② 卒業年月も「在学中」も無い行は、期間が「2005年4月 – 」で終わる~~（**2026-08-28 に表示側を修正**）

`EducationContent` が `graduated_at` も `is_current` も無いとき、**終わりを空のまま**
「2005年4月 – · 21年4ヶ月」と出していた。しかも `formatDuration(start, null)` は
**今日まで**で数えるので、**期間が毎月伸び続けていた。**

→ **「卒業年月 未入力」と出し、期間は出さない**ようにした。

⚠️ **「現在」と書いていない。** それは `is_current` の人にだけ言えること。
⚠️ **期間も出していない。** 終わりが分からないのに月数を出すと、伸び続ける数字を
   事実として見せることになる。

二択のうち「表示で出す」を採り、**「保存時に弾く」はしていない**。既存の実データが
1件あり（2026-08-28 実測 / 学歴12件中）、弾く側にすると**その人が学歴を保存できなくなる**
（`social_links` の URL 検証で同じ問題を検討したのと同じ形）。

**検証**（`is_test` の「鈴木 太郎」に入学年月だけの行を作り、ログインして `/u/` を実測）:
「2005年4月 – 卒業年月 未入力」と出て、「現在」にも伸びる期間にもならないことを確認。
⚠️ **検証行は削除して元の12件に戻した。**

### ✅ ①（入学年月が無い行が公開から消える）── **2026-08-28 に解消**

実データ **2件**（柴さん「朝霞・高校卒」／is_test「東京大学 経済学部」）。
`toTimelineEducationEntries` が `enrolled_at` の無い行を `filter` で落とすため
年表に置けず、**公開プロフィールから黙って消えていた。**

→ **年表の下に、年の分からない学歴として出す**ようにした（`u/[id]/page.tsx`）。
   学校ロゴ＋学校名＋（学部・学位）＋「**入学年月 未入力**」。**期間は出さない。**

⚠️★**必須化は採らなかった。** 必須にすると、既に登録している2人が
   **思い出せない年月の入力を強いられる**＝利用者に推測値を入れさせることになる
   （「推測値を投入しない」の裏返し）。②のときと同じ判断。

⚠️ `Education` 型に `school_id` / `school_master` を足した。**select では前から
   取っていたのに型に無く、使えなかった。**
   ⚠️★埋め込み（`school_master:ow_schools!school_id(...)`）は**配列で返る**ので、
      取得直後に畳んでいる。畳まないと受け手は `undefined` になり、
      **tsc も lint も通ったままロゴだけ黙って消える**（CLAUDE.md）。

**検証**（dev・ログインしてサーバー描画の実HTMLを数えた）:

| 対象 | 結果 |
|---|---|
| 入学年月が無い人（2名） | 学校名・学部・「入学年月 未入力」が**各1件**。学歴セクションの内側に出ている |
| 通常の学歴を持つ人（大塚悠貴・2件とも入学年月あり） | 「入学年月 未入力」**0件**・年表の年月表記10件＝**回帰なし** |

---

## /mypage のアクティビティに投稿の一覧が無い（2026-08-17 記録・未着手）

`/mypage` のプロフィールタブ下端「アクティビティ」にあるのは**投稿フォームだけ**。
`PostCard`（投稿の一覧）も削除メニューも無い（DOM とコードの両方で実測）。

そのため **`/u/[id]` の投稿カードにある「削除」は残してある**。消すと
`/feed` まで行かないと自分の投稿を消せなくなる。

⚠️ `/u/[id]` は 2026-08-17 に「他人からどう見えているか」を確かめる場所にしたので、
   **本人だけに見える「削除」が残っているのは、その方針からは外れている。**
   揃えるなら選択肢は2つ。

| 案 | 内容 | 気になる点 |
|---|---|---|
| A | `/mypage` のアクティビティに**投稿の一覧＋削除**を足し、`/u/[id]` からは消す | 一覧をどこまで出すか（件数・ページング）を決める必要がある |
| B | 現状のまま | `/u/[id]` に本人だけの操作が1つ残り続ける |

⚠️ 統合のとき「母校・アクティビティは残す」と決めた対象は**投稿フォーム**だった。
   一覧が無いのは決定と食い違っているわけではないが、**プレビュー化の方針とは食い違う。**

### ⚠️★2026-08-28 実測 ── **実ユーザーの投稿が0件**

| | 件数 |
|---|---|
| `ow_posts` 総数 | **170** |
| **うち実ユーザー（`is_test` / システムを除く）** | **0** |
| 投稿した実ユーザー | **0人** |

→ **案A（一覧＋削除を足す）を作っても、出るものが無い。**
   「一覧をどこまで出すか（件数・ページング）」も、判断材料が無いまま決めることになる。

⚠️ **`/u/[id]` に残っている「削除」も、実ユーザーには一度も押されていない**
   （押す対象の投稿が無い）。**急ぐ話ではない。**

**着手の条件**: 実ユーザーの投稿が生まれてから。そこで初めて
「何件出すか」「ページングが要るか」が実データで決まる。

---

## `ow_profiles.worry` は未使用列（2026-08-17 撤去。列は残す）

**UI（転職の希望の「今一番の悩み・相談テーマ」カード）と API の受け口（`VALID_WORRIES` ごと）は
撤去済み。`hasCareerPreferences` の判定からも外した。列とデータは残してある。**

理由: `ow_profiles.worry` を**読む画面が1つも無かった**。
マッチング（`scoreJob.ts`）・スカウト・企業側（`/biz`）・運営画面（`/admin`）・
通知・`/u/[id]` のどこも見ていない。「メンター相談やカジュアル面談のマッチングに使われます」と
画面に書いてあったが、**メンター機能自体が存在しない**（`ow_mentors` は migration 140 で DROP 済み）。

実測（2026-08-17 / `ow_profiles` 49件）:

| | 件数 |
|---|---|
| `worry` が入っている | **6** |
| うち**他の希望条件が全部空**で、`worry` だけで達成扱いだった | **3** |
| `hasCareerPreferences = true` | **6 → 3** |

⚠️ **3人は「転職の希望」タブが「未設定」に変わる。** 入力欄が無くなったので、
   その3人は希望条件を1つも入力していない状態が正しく出るようになる。

⚠️ **列は DROP していない。** 値の内訳は「転職すべきか迷っている」2件 /
   「年収を大幅に上げたい」4件。**読み手を決めてから**使うか消すかを判断する。

---

## フェーズ2（モーダル化）の積み残し（2026-08-17 記録）

### ✅ ① 職歴モーダルのチップが 36px だった（2026-08-28 解消）

入社理由・ギャップの**チップ**（「事業内容・プロダクト」「想像より良かった」など）が
高さ36px で、44px のタップ領域を満たしていなかった。
→ `ReasonChip`（`CareerHistoryEditor.tsx`）に **`minHeight: 44` + `inline-flex`** を入れた。

⚠️★**`::after` で見えない当たり判定を広げる案は測って落ちた。** 先に
   `top/bottom: -4.5px` の疑似要素を敷いたが、`elementFromPoint` が**当たらない**
   （`z-index: 0` を足しても、コンテナの `gap` を 6→9 に広げても変わらず）。
   **擬似要素はタップ領域の拡張として当てにならない。** 実箱を大きくすること。

実測（375px / `/mypage/details/experience` → 鉛筆 → モーダル / チップ46個のうち可視8個）:
**高さ44px以上 8/8 ・ 上端/中央/下端すべて当たる 8/8 ・ 高さの種類 [44] ・ 横スクロール無し。**

⚠️ **他のフォームのチップは未確認。** ここで直したのは職歴モーダルの `ReasonChip` だけ。

### ② `CardSaveFooter` は `WishesTab` で現役（5箇所）

プロフィールのカードからは全部消えたが、**転職の希望タブ**が
希望職種・勤務地・年収・フェーズの4カードで使っている（+1箇所）。

⚠️ **フェーズ2の対象外なので残した。** 設定・希望タブをどうするか（フェーズ4）で
   一緒に決める。`EditableSection` のほうは参照0になったので削除済み。

---

## ⚠️★アバターの職種別カラーが、全員フォールバックに落ちている（2026-08-31 発見・未決）

**`lib/jobCategoryColors.ts` の `JOB_CATEGORY_COLORS` の7キーが、
現在の `ow_roles` の親カテゴリ UUID と1つも一致しない。**

ファイルには「UUID は 2026-05 時点の ow_roles テーブル実値」とコメントされている。
その後 `ow_roles` が入れ替わったが、**この表は更新されなかった。**

| 職種 | 色表のキー | 実際の UUID（2026-08-31 実測） |
|---|---|---|
| エンジニア | `a905184b-…` | **`c8140123-…`** |
| 営業 | `89b056f4-…` | **`6938712f-…`** |
| マーケティング | `9ff6eb0c-…` | **`38429140-…`** |
| カスタマーサクセス | `093cd4bb-…` | **`ad47e554-…`** |
| 経営・CxO | `3b29af59-…` | **`166bebdf-…`** |
| PdM / PM | `15077bd6-…` | **該当なし**（現在は「プロダクト」`168cd1ab-…`） |

### なぜ誰も気づかなかったか

⚠️ **`resolveAvatarColor` は一致しないと `FALLBACK_CATEGORY_COLOR` を返す。**
落ちても例外にならず、**全員が同じ淡いグレー**になるだけ。
CLAUDE.md「403 は『0件』として静かに素通りする」と同じ形で、
**壊れていることが画面から分からない。**

実測（2026-08-31 / `/dev/preview/employees` / 37名）: 背景・文字色とも**1種類だけ**。

### 決めること

⚠️ **どちらを選ぶかは製品の判断。** 私（Claude）は変更していない。

| 案 | 得るもの / 失うもの |
|---|---|
| **A. このまま**（全員1色） | 一貫していて、**意味の無い色分けが無い**。⚠️ ただし一覧で人が見分けにくい |
| **B. UUID を直す** | 職種別の色が復活する。⚠️ **凡例がどこにも無い**ので、色の意味は読み手に伝わらない（`.claude/skills/ui-conventions`「凡例なしで意味が伝わるか」に照らすと弱い） |
| **C. 名前で引く** | UUID の入れ替えに強い。⚠️ 親カテゴリは**現在18件**あり、色は7つしか無い。11件は結局フォールバック |

⚠️ **B / C を採るなら、UUID をハードコードし直さないこと。** 同じことが繰り返される。
   ⚠️ **一致しなかったことを検知する手段**（起動時のアサートなど）とセットで入れる。
      無いと、次に `ow_roles` が変わった日にまた静かに戻る。

### 済ませてあること

**求人詳細のアバターを企業詳細と同じ `resolveAvatarColor` に揃えた**（2026-08-31）。
それまで求人詳細だけ `userId.charCodeAt(0) % 5` のハッシュで5色に塗っており、
**同じ人が2ページで別人のように見えていた**（濃いグラデ+白文字 vs 淡い背景+濃い文字）。

⚠️ これで**判定が1箇所になった**ので、上の A/B/C のどれを選んでも
   `lib/jobCategoryColors.ts` を直せば両ページが同時に直る。

---

## ⚠️ 記事の `related_job_ids` は、参照先が全滅していて、そもそも誰も読んでいない（2026-08-31 実測）

`ow_articles.related_job_ids` に **11 種類の求人 slug** が入っており、
**そのうち実在するものは0件**。参照している10記事すべてで死んでいる。

```
freee-csm / freee-engineer-platform / hubspot-solutions-engineer /
layerx-eng-backend / layerx-pdm-bakuraku / pksha-ml-engineer /
salesforce-ae-enterprise / sansan-pdm / smarthr-csm /
smarthr-eng-fullstack / ubie-backend-engineer
```

⚠️ **描画側からの参照が0件なので、画面には何も出ていない**（実害なし）。
   読んでいるのは `mapDbArticle` と型定義だけで、
   `articles/[slug]/page.tsx` は `related_article_slugs` しか使っていない。

### だから「関連求人」を作るときに、ここを拾わないこと

- **値が slug なのに列名が `_ids`。** `related_article_slugs` と対称でない
- **求人が消えても列は残る。** FK ではないので DB は守ってくれない
- **公開求人は2件しかない**（2026-08-30 時点）。10記事に2件を貼るなら
  「関連求人」ではなく別の見せ方になる

→ 作るなら **`ow_jobs.slug` を引いて実在するものだけ出す**
（`resolvePublishedCompanyHref` と同じ形。CLAUDE.md「企業ページへのリンクは
env に関係なく is_published を見る」）。

⚠️ **列は消していない。** 消すか作り直すかを決めてから触ること。

⚠️ `related_article_slugs` のほうは**9件すべて実在・公開中**で、正常に動いている。

### ✅ 企業が実在しない記事4件は削除した（2026-09-01 / migration 20260901190000）

`archi-village-cto-dx-journey` / `freee-platform-infra-report` /
`layerx-nakamura-why-mentor` / `layerx-suzuki-backend-career`。
企業側は migration 238/239 で削除済みだったが、**記事だけが公開されたまま残っていた。**

⚠️★**`ow_posts.ref_article_id` に FK が1本も無い。** 記事だけ消すと投稿4件が
   参照先を失い、**幽霊投稿になる**（238/239 で作った「幽霊投稿60件」と同じ形）。
   → 投稿も同じ migration で消した。いいね0 / コメント0 / 通知0 は事前に確認済み。

⚠️ 他の記事6件が `related_article_slugs` でこの4件を指していたので、配列から取り除いた。

実測（適用後）: 記事 16 → **12** ／ 投稿 170 → **166** ／ 可視投稿 110 → **106** ／
企業が無い記事 **0** ／ 関連記事が切れている記事 **0**。

---

## ⚠️★`--success` の文字色は、`--success-soft` の上で 3.58 ── 31箇所（2026-08-31 実測）

```
--success:      #059669   /* 文字 */
--success-soft: #ECFDF5   /* 背景 */
→ コントラスト比 3.58
```

**12px / 13px の文字に必要なのは 4.5**（3.0 が使えるのは 18.66px 以上の太字か 24px 以上）。
`var(--success)` を文字色、`var(--success-soft)` を背景に使っている箇所は **31**。
**そのすべてが基準を割っている。**

実際に見た例: `/u/[id]` の職歴タイムラインの **「在籍中」バッジ**（12px 太字）。
2026-08-31 に `/feed` の投稿種別バッジを直したときと**同じ色の組み合わせ**。

### 直し方の候補

⚠️ **`--success` をそのまま濃くしないこと。** この変数は**塗り**にも使われている
   （`globals.css:1632` の在籍中パルスドット、ボーダー、アイコン）。濃くすると塗りも変わる。

| 案 | 内容 |
|---|---|
| **A** | **`--success-ink: #047857` を足し、文字だけそれに差し替える**（31箇所）。実測 **5.21** ✅ |
| B | バッジの背景を白にして枠線で見せる（`--success` は枠だけ） |
| C | 12px をやめて 18.66px 太字にする（現実的でない） |

**A を推す。** `--ink-mute` が「本番視認性改善のため1段濃く」された前例と同じ形。

✅ **`--warm` も 2026-09-01 に同じ形で解決した。** `--warm-ink: #B45309` を足し、
   文字色としての `--warm` を **0箇所**にした（残り12箇所はすべて塗り・枠・ドット・stroke）。

⚠️ **色の役割の整理（`--warm` が5つの無関係な意味を運んでいる件）は別のまま。**
   今回やったのは**読めるようにしただけ**で、意味の整理はしていない。

---

## ⚠️ カジュアル面談CTA は 白 on `#F59E0B` = 2.15（2026-08-31 実測・サイト全体）

```
linear-gradient(135deg, #F59E0B, #FB923C) の上に 白・13px
→ 明るい側のストップ #F59E0B で 2.15（必要 4.5）
```

⚠️ **これはブランドの主要CTA**（ui-conventions「オレンジ = カジュアル面談のみ」）なので、
   **1画面の判断で色を変えない。** `/u/[id]`・`/people`・企業ページ・求人ページに出る。

直すなら選択肢は3つ。**どれもブランドの見え方が変わるので、決めてから一斉にやること。**

| 案 | 内容 |
|---|---|
| A | **文字を濃色にする**（白をやめる）。オレンジは保つ |
| B | **オレンジを1段濃くする**（`#B45309` なら白で 4.8）。ただし別の色に見える |
| C | 文字サイズを上げる（13px → 18.66px 太字で基準が 3.0 になるが、それでも 2.15 で足りない） |

⚠️ **C は成立しない。** 2.15 は 24px でも基準（3.0）に届かない。**色を変えるしかない。**

---

## ⚠️★社名を伏せる手段が、いま1つも無い（2026-08-31 確認）

`ow_experiences.visibility_company` / `visibility_company_profile`（`real` / `masked` / `hidden`）
は**列も RLS も描画側のフィルタも生きている**が、**値を選ぶ UI が存在しない。**

| いつ | 何が起きたか |
|---|---|
| 2026-08-14 | オンボーディングの「会社名は伏せる」チェックを削除。理由は「**あとから `/mypage` で変えられる**」 |
| **2026-08-16** | **その `/mypage` 側（`CareerHistoryEditor`）の「公開設定」入力欄を削除** |

**2日ずれで、根拠にしていた逃げ道のほうが先に無くなった。**
`CareerHistoryEditor.tsx` には「★入力欄が無い＝**本人が掲載を断る手段が無い**」と
書いてあり、オンボーディング側には「**列も編集UIも消していない**」と書いてある。
**2つのコメントが2週間食い違ったままだった**（オンボーディング側は 2026-08-31 に訂正した）。

実測（2026-08-31 / 本番）: `ow_experiences` **24件すべて** `real`。
⚠️ **これは「誰も伏せたがらなかった」ではなく「選べなかった」。**

### 戻すときの注意

⚠️ **1件ずつ選ばせる形に戻さないこと**（`CareerHistoryEditor.tsx` の注意書き）。
   選び忘れが同意なき公開になる。「職歴全体をどう見せるか」の1設定として設定タブに置く。

⚠️ 実ユーザー6人は**全員 `ow_users.visibility = 'login_only'`** なので、
   現状でも未ログインには誰も見えない（実測で `/people` `/u/[id]` とも 307 → `/auth`）。
   **いま漏れてはいない。** 足りないのは「ログイン済みの他人に社名を見せない」選択肢。

---

## ⚠️ `KpiCard` のアイコン背景は最初から出ていない（2026-08-31 発見・未修正）

`/biz/analytics` の `KpiCard`（`page.tsx:107`）は、渡された色から背景を作ろうとして

```ts
background: `${color}18`,   // ← 16進アルファを後ろに足すつもり
```

と書いているが、**渡ってくるのは `var(--royal)` のような CSS 変数参照**なので
`var(--royal)18` になり、**CSS として不正 → 宣言ごと破棄 → 背景は透明。**

⚠️ **既定値も `color = "var(--royal)"` なので、全カードで効いていない。**
   `--success-ink` への差し替え（2026-08-31）で見つけたが、**差し替え前から同じ**。
   16進リテラルを渡している呼び出しは1つも無い。

直すなら `color-mix(in srgb, ${color} 9%, transparent)` か、`bg` を別の props で受ける。
⚠️ **`${color}18` の形を他所にコピーしないこと。**

---

## ⚠️ 白文字を `--warm` / `--success` の**塗り**に載せている箇所（2026-08-31 記録）

`--success-ink` の追加（2026-08-31）で直したのは**文字色として使っていた側**。
**塗りの上に白文字を置いている側は別で、まだ残っている。**

| 塗り | 白文字の比 | 必要 |
|---|---|---|
| `--warm` #F59E0B | **2.15** | 4.5（大きい文字でも 3.0 に届かない） |
| `--success` #059669 | **2.93** | 4.5 |

実際に見た例（`/companies/[id]`・**本人にだけ出る掲載設定バナー**）:

```
CompanyEmployeeSections.tsx の tone / toneSoft
  掲載中   … tone: var(--success) + 白文字の「掲載設定を変更する→」 → 2.93
  非掲載   … tone: var(--warm)    + 白文字の「掲載設定を変更する→」 → 2.15
```

⚠️ **カジュアル面談CTAは 2026-08-31 に解決済み**（`MEETING_CTA_BG` = #C2410C → #9A3412）。
   ここに書いているのは**それ以外**。

### 直すときの注意

⚠️ **`--success-ink` を塗りに使わない。** あれは「明るい背景に載せる文字」用で、
   白文字を載せる背景には暗すぎない（#047857 に白 = 5.48 なので実は使える）が、
   **役割の名前と合わない。** 塗り用に必要なら別名で足すこと。

⚠️ **`--warm` は `globals.css` 自身が「5つの無関係な意味を1色で運んでいる（37箇所）。
   新しく使わないこと」と書いている。** 色の役割の整理と同時にやるほうがよい。

### 別件: 画像の上のラベル

`/companies/[id]` の「サンプル写真」は `rgba(0,0,0,0.5)` のオーバーレイに白文字で **3.98**。
⚠️ **写真によって実効値が変わる**ので、他とは別の扱いが要る（オーバーレイを濃くするか、
文字に影を付けるか）。**いまは触っていない。**

---

## ✅ ~~`/biz`（企業側）はモバイル幅で崩れる~~（2026-08-31 に実測 → **同日対応済み**）

実測（`/biz/dashboard` / 375px / ログイン済み）:

| | |
|---|---|
| 画面外に出る要素 | **14** |
| 企業切り替えボタン | 右端 **443px**（画面 375px） |
| ユーザーメニュー | 右端 **601px** |
| ページの横スクロール | **出ない**（`body { overflow-x: hidden }` が隠す） |

⚠️★**つまり切り取られている。** 企業の管理者がスマホで開くと、
**企業の切り替えとユーザーメニューに触れない。**

⚠️ ダッシュボードの2カラム（`gridTemplateColumns: "1fr 1fr"`）が**375pxでも2列のまま**で、
   各カラムが 163px しかない。そのなかに求人ステータスの5枚が縦積みになる。

### 直すなら

- `/biz` 全体のレスポンシブ対応になる（ヘッダー・サイドバー・2カラム）。**1画面の話ではない。**
- ⚠️ **そもそもモバイルを支える方針かを先に決める。** 採用管理は PC 前提の運用もありうる。
  支えないなら「PCでご利用ください」と**出す**（黙って切り取らない）。

⚠️ `scrollWidth` では見つからない。**各要素の右端を画面幅と比べること**
   （`.claude/rules/ui-debugging.md`「横はみ出し」）。

### ✅ 対応した（2026-08-31）── 「支えない」ではなく「実装の漏れ」だった

⚠️★**方針を決め直す必要は無かった。** `BusinessLayout` には**元から ≤768px の対応があった**
（サイドバーを畳む）。つまり「モバイルを支える」は既に決まっていて、
**ヘッダーとページ内の多カラムが漏れていた**だけだった。

| 何が | 直し方 |
|---|---|
| ヘッダーが `@media` の**対象外**だった | `.biz-header` ほかにクラスを付け、768px で詰め、480px で BUSINESS バッジと氏名を畳む |
| サイドバーの `flex-direction: row` が**効いていなかった** | `nav` が素の `display:block`。**`display:flex` を先に当てる**必要があった |
| 企業カードのロゴが社名に**重なっていた** | 列幅を 48px にしただけでは足りず、**ロゴ本体（64px）も縮める** |
| ページ内の2列・3列が畳まれない | `.biz-2col` / `.biz-3col` を globals.css に置き、実測で壊れた箇所に付与 |

実測（375px / 切り取られている要素の数）:
`/biz/dashboard` `/biz/jobs` `/biz/company` `/biz/meetings` `/biz/analytics`
`/biz/members` `/biz/posts` `/biz/organization` — **すべて 0件**。
1280px では sidebar 240px / header padding 12px 28px / 企業カード 64px 1fr auto と**元のまま**。

✅ **残り4ページも測った（2026-09-01）。** `/biz/employees` `/biz/conversations`
   `/biz/scouts` `/biz/candidates` ── **4ページとも切り取り0件・縦潰れ0件。**
   ＝ **`/biz` の12ページすべてで 375px の切り取りは0**になった。

⚠️ 多カラム28箇所のうちクラスを付けたのは**8箇所のまま**。残り20箇所は
   **測って壊れていなかった**か、**そのページに出ていなかった**かのどちらか。
   新しいページや条件分岐で出てくる可能性はある。

⚠️★**`BusinessLayout` の `<style>` ブロックにバッククォートを書かないこと。**
   テンプレートリテラルがそこで閉じ、以降が JSX として解釈される（同日に踏んだ）。
   CLAUDE.md の「`<style>` 内で `>` と引用符を使わない」と**同じ場所の別の罠**。
   → だから `/biz` のレスポンシブは **globals.css**（素の CSS）に書いている。


---

## ⚠️ 白文字を塗りに載せている箇所 ── 一部だけ直した（2026-09-01）

`--warm-ink` / `--success-ink` の移行は**文字色側**の話。
**塗りの上に白文字**を置いている側は別で、まだ残っている。

| 塗り | 白文字の比 |
|---|---|
| `--warm` #F59E0B | **2.15** |
| `--success` #059669 | **2.93** |

✅ 直した: `/biz/members` の「招待中 N」バッジ（11px・2.15 → **5.02**。塗りを `--warm-ink` に）

⚠️ **残っている**: `/companies/[id]` の**本人にだけ出る掲載設定バナー**
（`CompanyEmployeeSections` の `tone` / `toneSoft`。掲載中＝`--success` で 2.93 ／
非掲載＝`--warm` で 2.15）。ほか未点検の箇所もありうる。

⚠️ **`--success-ink` / `--warm-ink` を「塗り用」として使い回さないこと。**
   `/biz/members` で塗りに使ったのは、**白文字を載せて 5.02 になる**ことを実測したうえでの
   個別判断。名前は文字色を指しているので、増やすなら塗り用の名前を別に立てる。

---

## `/admin`（運営画面）の点検 ── 2026-09-01

### ✅ 直した2件

**① ダッシュボードの数字が「何を数えたか」を言っていなかった**

「登録ユーザー数 38」「オンボーディング完了率 79%」と出していたが、
実測で **38人中32人が検証用アカウント**。実ユーザーは6人で、
**実ユーザーだけのオンボーディング完了率は 50%（3/6）。**

⚠️★**コード側は知っていた。** `admin/page.tsx` に
「実ユーザーは6人。この数字を対外的な指標に使わないこと」と**コメントで書いてあった**が、
**画面には一切出ていなかった。** 見た人には区別する手段が無い。

→ KPI に内訳（`うち検証用 32 ／ 実ユーザー 6`）、ファネルに注記を出した。
⚠️ **母数から検証用を外す案は採らない**（運営画面はテストデータも見えるほうが正しい。
CLAUDE.md「完全に隠さないこと」／`/admin/ambassador-requests` と同じ方針）。

**② ~~学校追加リクエストが「要対応」に入っていなかった~~（⚠️ 同日にこの機能ごと畳んだ）**

⚠️★**この項目で私が書いた「承認するまで、その人の学歴が登録できません」は誤りだった。**
   実際は**マスタ承認を待たずに保存される**（`ow_user_educations` は
   `school`(text) と `school_id`(nullable) の両方を持つ「マスタ＋自由入力」の
   ハイブリッド。LinkedIn と同じ）。**誰も待たされていなかった。**
   → 2026-09-01 に**要対応の行ごと撤去**し、リクエスト機能も畳んだ（下記）。

以下は当時の記述（経緯として残す）:


実測: **pending 3件・最古74日前**（2026-06-19 から）。
`/admin/school-requests` を**直接開かないと誰も気づけない**状態だった。

⚠️ これは求職者が送ってきたもので、**承認するまでその人の学歴が登録できない。**
→ 要対応タスクに行を足した（5件 → **8件**）。

### ⚠️ 見つけたが直していない

- **「メンター相談転送待ち」の行が残っている。** `pendingReservationsCount: 0` の**直書き**で、
  メンター機能自体が存在しない（`ow_mentors` は migration 140 で DROP 済み・参照0件）。
  ⚠️ **数え直そうとしないこと。** 消すかどうかは別途判断。

### 問題が無かったもの

- **`/admin/ambassador-requests` はよくできている。** 5名すべて出し、検証用にラベルを付け、
  ON からの日数を出し、「この企業は通知の宛先が0件です」まで警告している
- `/admin/jobs` の「すべて 18」は**テスト求人2件を専用タブに分けた結果**で、設計どおり
  （CLAUDE.md「完全に隠さないこと」）
- `undefined` / `NaN` / `[object Object]` / `Invalid Date` は **19ページすべてで0件**
- ダッシュボードの数字（38 / 12 / 85 / 2）は**すべて DB と一致**
  （85 は `is_published`。一覧掲載は79で、**別の軸**）

⚠️ 私の誤検知: `***@example.com`（メールのマスキング）を「生の markdown」と数えた。
   **正しい実装**だった。

---

## `/mypage`（求職者の自分の画面）の点検 ── 2026-09-01

### ✅ 直した4件

**① ★設定ページが、同じページ上の設定を「別のページで変えろ」と案内していた**

`/mypage/settings` の冒頭に
「プロフィールの公開範囲は**マイページ**の**『転職の希望』**から変更できます」とあったが、
**3つとも事実ではなかった**:

- 公開範囲は 2026-08-20 に**このページの上部へ移した**
  （`settings/page.tsx` に「本文の『転職の希望』から移した。両方に置かないこと」と書いてある）
- `/mypage` に「転職の希望」という見出しは**無い**（いまは「意思表示」）
- `/mypage` に公開範囲の設定は**無い**

⚠️ **同じページの 200px 上にある設定を、別のページへ探しに行かせていた。**

**② ★スカウトの空状態が「本人のプロフィール不足」と読めた**

「まだスカウトは届いていません／**プロフィールに経歴や希望条件を入れておくと、
企業から見つけてもらいやすくなります**」を、送信が止まっている状態でも出していた。

⚠️ 実測（2026-09-01）: `ow_scouts` **0件**、`SCOUT_SENDING_ENABLED` **未設定**。
   受け取る設定にしている人（`scout_enabled = true`）が **17人**いても**1通も届きようがない。**
⚠️ **2026-08-31 に `/biz/scouts`（企業側）を直したのと同じ理由。その対。**
   企業側にだけ「まだご利用いただけません」と出して、求職者側に
   「あなたのプロフィール次第」と言い続けるのは筋が通らない。

**③ `--error-ink` を足した（`--error-soft` の上でだけ足りない）**

⚠️ `--success` / `--warm` とは**壊れ方が違う**。
on #FFFFFF = **4.83 ✅** ／ on --bg-tint = 4.62 ✅ ／ **on --error-soft = 3.95 ❌**
→ **全面移行しない。** 淡い赤の上の赤文字だけ（1行で対になっている23箇所＋手で1箇所）。

**④ `/mypage/applications` の4つの数字を揃えた**

黄 `#CA8A04`（24px で **2.94**・基準3.0）／紫 `text-purple-600`（ui-conventions で**紫は使わない**）
／濃紺／灰 の4色に**凡例が無かった**。全部 `--ink` に。
⚠️ 1つだけ色を残す案は採らない（序列になる。`EMPLOYMENT_BADGE` の前例と同じ）。

### 問題が無かったもの

- **メッセージのバッジは正しい。** 「7日以内に動きがあった会話数」で、
  一覧と**同じ基準**（参加者行）。CLAUDE.md 2026-08-20 の「存在しない列で数えていた」は解消済み
- **過去のメールのリンクは全部生きている。** `/profile/edit?tab=...` → `/mypage?tab=...` →
  `/mypage/settings` と2段で転送され、`?tab=settings` `?tab=account` `?tab=privacy` すべて着地
- `undefined` / `NaN` / `Invalid Date` は8ページすべてで0件

### ⚠️ 残り

- `--error` の**複数行にまたがる**組み合わせが27件候補に出たが、
  **±3行の窓で拾っただけで、実際に淡い赤の上にあるとは限らない。**
  実測で出たものだけ直した。**残りは未検証**（＝壊れているとは言っていない）
- `STATUS_CONFIG`（応募ステータスのバッジ色）は**応募0件で描画されず未測定**

---

## LP・残りの公開ページの点検 ── 2026-09-01（求職者側の点検はこれで一巡）

対象: `/` `/careers` `/contact` `/search` `/schools/[id]` `/jobs/dept/[slug]`

### ✅ 直した5件

**① ★根拠の無い年収レンジ6件を `/careers` から外した**

`ROLE_MAP` に「エンタープライズ営業 800〜1,800万円」など**6職種の年収が直書き**されていた。

⚠️ 実測（2026-09-01）: **公開求人は2件だけ**（どちらも Salesforce）。
**6職種のうち5職種は、根拠になる求人が1件も無い。**

⚠️ 年収データベース（`/salary`）は `0c06c6ec`（2026-08-29）に
「年収データを増やす予定が無い」という理由で削除済み。そのとき meta・FAQ・見出し下の
文言は消したが、**ここだけ残っていた。**

⚠️ OPINIO は有料職業紹介事業の許可事業者。出典の無い数字は**的確表示義務**に関わる。
⚠️ **求人から算出して出し直さないこと。** 2件では職種別の相場にならない。

**② `/careers` の STEP が 01 / 02 / 04（03 が欠番）だった**

STEP 03「年収相場を把握する」が `/salary` 削除時に外され、**番号と見出しが取り残された。**
画面は「**4 ステップ**」と言いながら **3枚**を出していた。
→ 03 に詰め、見出しは `STEPS.length` から出す（直書きしない）。

**③④ `/careers` の色** — STEP 02 が `#059669`（白の上で **3.77**）、STEP 03 が**紫**。

**⑤ ★★`/schools/[id]` が未ログインに「出身者はまだ登録されていません」と断定していた**

実測（獨協大学）: **未ログイン「出身者の登録はありません」／ログイン済み 2名**。

⚠️ 実ユーザーは**全員 `login_only`** なので、未ログインには必ず0件になる。
`getGraduates` は `isLoggedIn` を受けて**正しく絞っている**のに、
**文言だけがその事実を知らないまま断定していた。**
⚠️ **公開ページが事実でないことを言っていた。**

⚠️ 未ログインには**件数も出さない**（0 と出すこと自体が「居ない」の主張になる）。
居るか居ないかも言わない（`login_only` の人の存在を漏らさない）。

⚠️★**`/search` は元から「人: ログインすると表示」と正しく出していた。**
   `/schools/[id]` だけが例外だった。**この言い方を規約として揃えること。**

### ⚠️ 判断待ち ── LP の FAQ

「登録すると、スカウトが届きますか？」の答えは**条件としては正確**だが、
**スカウトがまだ開いていないことに触れていない**（`SCOUT_SENDING_ENABLED` 未設定・
`ow_scouts` 0件）。

⚠️ **公開LPの文言なので勝手に変えていない。** いつ開くかの判断と対で決めるもの。
2026-08-31 に `/biz/scouts`、2026-09-01 に `/mypage/scouts` を同じ理由で直しており、
**LP だけが残っている。**

### 問題が無かったもの

- `/` は 1280px / 375px とも**コントラスト不足0・切り取り0**。
  業種ファセットの件数（12+12+2+8+10+8+7+6+3+7+2+2 = 79）は掲載企業数と一致
- `/contact` `/search` `/jobs/dept/[slug]` も0件


---

## ✅ 学校追加リクエストを畳んだ（2026-09-01 / 柴さんの判断）

### なぜ ── 実績が0だった

実測（2026-09-01 / 本番）:

| | |
|---|---|
| リクエスト全件 | **3**（⚠️ **すべて 2026-06-19 の同じ日**） |
| 承認 / 却下 | **0 / 0**（74日放置） |
| ⚠️ **リクエスト由来の学校** | ⚠️ **0件** |
| 学校マスタ | 37（**すべて migration 由来**） |
| 学歴 | 12件（マスタ紐づけ10 / **自由入力2**） |

中身は「**あ**」「**朝霞**」など**入力途中の断片**か、「**東京都立大学**」のように
**既にマスタにあるもの**だった。3件とも `rejected` にした（`20260901221000`）。

⚠️★**運営に承認の手を求めながら、マスタに1件も貢献していなかった。**
`/salary` を消したとき（`0c06c6ec`）と同じ構図。

### ★誰も待たされていなかった（当初の私の記述は誤り）

⚠️ `ow_user_educations` は `school`(text, NOT NULL) と `school_id`(nullable) の**両方**を持つ。
**LinkedIn と同じ「マスタ＋自由入力」のハイブリッド**で、マスタに無い学校でも
**そのまま保存され、プロフィールに出る**（実例: 柴さんの「朝霞」が `school_id = null` で保存済み）。
マスタに紐づくと**ロゴと `/schools/[id]` へのリンクが付く**だけ。

### 消したもの / 残したもの

| | |
|---|---|
| 消した | 求職者側のバナー（`RecordEditors`）／ `/api/jobseeker/school-requests` ／ `/admin/school-requests` ／ 承認・却下API ／ 管理サイドバーのリンク ／ `/admin/schools` の案内文 ／ **今日入れた要対応の行** |
| **残した** | ⚠️ **テーブル `ow_school_requests` と3行**（使われた記録）。**DROP していない** |

⚠️ **バナーを戻さないこと。** 戻すなら「**入力途中の断片を送れない**」ようにするのが先。
⚠️ 学校は **migration で足す**（マスタ37件はすべてその経路）。

### ⚠️ 残る論点

**`/schools/[id]` の位置づけ。** マスタに紐づく価値は「ロゴ＋学校ページ」だが、
その学校ページは**未ログインに何も見せない**（実ユーザーが全員 `login_only` のため。
2026-09-01 に「ログインすると表示されます」に直した）。
**学校ページで何を見せたいのかが決まっていない。**
